# Implementation Ledger

This is the authoritative, append-only record of feature-by-feature implementation status for CIPHER. Every accepted implementation increment must have a row here.

## Legend

- **Status**: `Completed` / `In Progress` / `Not Started` / `Blocked`
- **Owner/AI**: who or what implemented the item (human name, or the AI agent/tool used)
- **Git Commit**: the commit hash that introduced/completed the item. Recorded as `Pending` when the item is done but not yet committed — a hash is never invented.
- **Visual Proof**: link to the screenshot/recording under `docs/progress/`, or `N/A` for items that produce no visual surface (e.g. environment setup)
- **Test**: whether the item has an associated automated test, or `N/A`
- **Demo Ready**: whether the item is ready to be shown in the demonstration flow

## Ledger

| ID | Feature | Status | Owner/AI | Git Commit | Visual Proof | Test | Demo Ready |
| --- | --- | --- | --- | --- | --- | --- | --- |
| P0.1 | Mac environment verification (macOS, Apple Silicon, resources) | Completed | Human (devv) | `df66560` | N/A | N/A | No |
| P0.2 | Xcode Command Line Tools installed | Completed | Human (devv) | `df66560` | N/A | N/A | No |
| P0.3 | Homebrew installed | Completed | Human (devv) | `df66560` | N/A | N/A | No |
| P0.4 | Baseline development utilities / Docker verified | Completed | Human (devv) | `df66560` | N/A | N/A | No |
| P0.5 | Canonical public GitHub repository created (`devv0311/netintel-ai`) | Completed | Human (devv) | `df66560` | N/A | N/A | No |
| P0.6 | Repository directory structure established | Completed | Human (devv) | `df66560` | N/A | N/A | No |
| P0.7 | `.gitignore` populated | Completed | Claude Code (AI) | `df66560` | N/A | N/A | No |
| P0.8 | `.env.example` populated | Completed | Claude Code (AI) | `df66560` | N/A | N/A | No |
| P0.9 | `README.md` populated | Completed | Claude Code (AI) | `df66560` | N/A | N/A | No |
| P0.10 | `LICENSE` established (MIT) | Completed | Claude Code (AI) | `df66560` | N/A | N/A | No |
| P0.11 | Governance documentation created (`docs/*/README.md`) | Completed | Claude Code (AI) | `df66560` | N/A | N/A | No |
| P0.12 | Implementation ledger created (this document) | Completed | Claude Code (AI) | `df66560` | N/A | N/A | No |
| P0.13 | Progress evidence convention documented | Completed | Claude Code (AI) | `df66560` | N/A | N/A | No |
| P0.18 | GitHub repository governance (branch protection on `master`: block force-push, block deletion; Dependabot vulnerability alerts enabled; `docs/repository-governance.md` authored) | Completed | Claude Code (AI) | `ca693ec` | N/A | Validated via `gh api` branch-protection and security settings checks | No |
| P1.1 | Requirements specification (`docs/requirements.md`) | Completed | Claude Code (AI) | `fea8622` | N/A | N/A — specification document | No |
| P1.2 | Synthetic investigation specification — Operation DarkNet Delhi (`docs/data/synthetic-investigation-spec.md`) | Completed | Claude Code (AI) | `fea8622` | N/A | N/A — specification document; no data generated | No |
| P1.3 | Ground-truth specification (`docs/data/ground-truth-spec.md`) | Completed | Claude Code (AI) | `fea8622` | N/A | N/A — specification document; no ground truth created | No |
| P1.4 | Agent contract specification (`docs/contracts/agent-contracts.md`) | Completed | Claude Code (AI) | `fea8622` | N/A | N/A — specification document | No |
| P1.5 | End-to-end demo contract (`docs/demo/demo-contract.md`) | Completed | Claude Code (AI) | `fea8622` | N/A | N/A — specification document; Copilot not implemented | No |
| P1.6 | Evaluation specification (`docs/evaluation/evaluation-spec.md`) | Completed | Claude Code (AI) | `fea8622` | N/A | N/A — specification document; no evaluator implemented | No |
| P2.0 | Master implementation blueprint (`docs/implementation-blueprint.md`) — milestones, task decomposition, schedule, delegation matrix, risk register | Completed | Claude Code (AI) | `50c3ba8` | N/A | N/A — planning document | No |
| P3.1 | Technology stack selection — ADR-001 (`docs/architecture/technology-stack.md`) and stack contract (`docs/architecture/stack-contract.md`); hardened in P3.2 (SQLite driver corrected to `node:sqlite`, LLM cache key strengthened to (model, prompt version, schema version, input), Docker resource-strategy clarified as optional-not-required) | Completed | Claude Code (AI) | `e69d5bc` (original decision); `24031f8` (P3.2 hardening) | N/A | N/A — architecture decision record | No |
| M1 | Technology Stack Selection & Environment Bootstrap | PARTIAL — stack selected (P3.1); application skeleton, DB init, typed boundaries (P4.1), and the full domain/data model + migrated schema (P4.2) now exist; the LLM response cache (model+prompt+schema-versioned) is still PLANNED | Unassigned | — | Required at completion | Required at completion | No |
| P4.1 | Application foundation bootstrap — Next.js App Router skeleton, SQLite (`node:sqlite` via Drizzle 1.0 RC) with a migrated placeholder table, typed provenance/evidence-classification and pipeline-stage boundaries (no stage logic), lazy AI client boundary (no key required at startup), graphology wiring, shadcn/ui-pattern shell (header, nav, pipeline-status strip, empty state), Vitest + Playwright configured | Completed | Claude Code (AI) | `e0b1566` | `docs/progress/evidence/P4.1/P4.1_screenshot_2026-09-02_e0b1566.png` | 4/4 Vitest unit tests pass; 1/1 Playwright E2E test passes (zero console errors); `tsc --noEmit` clean; `eslint .` clean; `next build` succeeds | No — foundation only, no pipeline stage implemented |
| P4.2 | Domain/data foundation — 14 Zod-validated domain types (`src/lib/domain/`: investigation, evidence source, evidence item, extracted record, entity, alias, location, temporal interval, communication event, financial transaction, relationship, analytical signal, AI inference, investigative lead) with confidence kept separate from the 5-value evidence classification; deterministic content-addressed + opaque ID generation (`ids.ts`); a validated repository layer (`src/lib/db/repository.ts`) over a 13-table migrated SQLite schema (`schema.ts`, adding 13 domain tables on top of the P4.1 `app_meta` placeholder); a synthetic-fixture + isolated ground-truth-fixture loading boundary (`src/lib/fixtures/`, `evidence/*/fixtures/foundation-smoke*`) — not the full Operation DarkNet Delhi dataset | Completed | Claude Code (AI) | `0b6c7c3` | N/A — backend/domain foundation only, no new UI surface | 35/35 Vitest unit tests pass (7 files: ids, domain-validation, provenance, db, fixtures, env, graph); 1/1 Playwright E2E test still passes unmodified; DB smoke test (empty→migrate→insert→select→verify provenance→re-migrate) passes; `tsc --noEmit` clean; `eslint .` clean; `next build` succeeds; no dependency changes | No — foundation only, no pipeline stage implemented |
| P5.1 | Synthetic investigation corpus — Operation DarkNet Delhi (blueprint Workstream A / tasks A1–A5). Deterministic generator (`src/lib/corpus/`: seeded mulberry32 PRNG, case design, synthetic-identifier minting, canonicalisation) producing corpus version `1.0.0` / seed `20260901`: 1,820 application-evidence items across 6 sources (5 FIRs, 8 primary suspects + registry records, 1,150 CDRs, 560 financial transactions, 10 witness statements, 4 crime events, 14 locations) committed as `evidence/synthetic/operation-darknet-delhi.json` in the P4.2 fixture format (extended additively — no second format); 1,150 structured communication events + 560 structured financial transactions + 14 locations projected with full provenance; a held-out ground-truth answer key (`evidence/ground-truth/operation-darknet-delhi.ground-truth.json`) covering every `docs/data/ground-truth-spec.md` §3 category, loaded only by `src/lib/corpus/ground-truth.ts` and unreachable from the evidence path. Application-evidence loader (`loadInvestigationCorpus`) + isolated ground-truth loader + `persistCorpus` (repository-layer only) + deterministic `validateCorpus` routine. All spec §4 structural properties present (aliases, duplicate/ambiguous identities, 3 contradictions, indirect + intermediary + money-mule + misleading-noise relationships, 4 temporal + 3 spatial correlations, 1 known hidden S1↔S4 connection). `docs/data/corpus.md` documents version/seed/counts/procedures/boundaries. | Completed | Claude Code (AI) | `df60d05` | No new visual evidence required — synthetic evidence corpus/backend foundation only, no new UI surface | 38 new Vitest tests in `tests/unit/corpus.test.ts` (73/73 total across 8 files): 4 volume floors; 9 structural properties; synthetic safety; ground-truth isolation (source scan + no-leak); schema rejection of malformed input; deterministic regeneration (canonical fingerprint pins + committed-file sync); full DB path (empty→migrate→load→persist→verify counts→verify provenance round-trip). `tsc --noEmit` clean; `eslint .` clean; `next build` succeeds; `playwright test` 1/1 unchanged; `git diff --check` clean; secret scan clean; no dependency changes. | No — evidence corpus only, no pipeline stage implemented |
| P5.2 | Evidence ingestion workflow — the first real investigation workflow (blueprint Workstream B / task B1 + the ingestion half of B3). A typed 8-stage ingestion service (`src/lib/ingestion/`: `input → file_validation → schema_validation → normalization → id_assignment → provenance → persistence → result`) returning a structured `IngestionResult` (never throws for expected failures). Input = the P5.1 built-in corpus (or an uploaded corpus manifest in the same P4.2 fixture format — no second representation). Normalization reuses `materializeCorpus`, preserving original evidence identity/source/location/type/timestamps/structured fields/provenance/validation-state and inventing nothing; deterministic content-addressed IDs; full provenance on every structured row tracing to a source evidence item; ingestion output never classified as AI inference. Idempotent: an `app_meta` completion marker + row-level "skip existing deterministic id" (first run 3,551 created / 0 skipped; repeat run `already_ingested`, 0 writes; partial-failure retry inserts only missing rows) — persistence is repository-layer only, no schema change. Ground-truth isolation: built-in source hardcoded to `evidence/synthetic/`, no ingestion module imports the ground-truth loader, uploaded answer-key files rejected `GROUND_TRUTH_REJECTED` with nothing persisted. Structured, user-safe error taxonomy (`INVALID_FIXTURE`, `MALFORMED_EVIDENCE`, `UNSUPPORTED_EVIDENCE_TYPE`, `VALIDATION_FAILURE`, `GROUND_TRUTH_REJECTED`, `PERSISTENCE_FAILURE`, `INTERNAL_ERROR`) — no stack traces / paths / secrets to the client. Streaming route handler `POST /api/ingestion` (newline-delimited real stage events) + `GET` for `InvestigationState`. UI: the placeholder disabled upload is replaced by a real workflow — empty workspace → Start ingestion → live 8-stage progress → loaded investigation summary with deterministic counts (6 sources / 1,820 items / 1,150 communications / 560 transactions / 14 locations) + per-type breakdown; synthetic-data-only indicator throughout; analysis/Copilot/dossier nav stay disabled; `docs/data/ingestion.md`. | Completed | Claude Code (AI) | `6795b5e` | `docs/progress/evidence/P5.2/` — `P5.2_screenshot-empty_2026-09-02_6795b5e.png` (empty workspace), `P5.2_screenshot-progress_2026-09-02_6795b5e.png` (live 8-stage ingestion), `P5.2_screenshot-loaded_2026-09-02_6795b5e.png` (investigation loaded + evidence counts). Captured by `CAPTURE_EVIDENCE=1 npx playwright test ingestion.spec.ts` against the real running app. | 22 new Vitest tests in `tests/unit/ingestion.test.ts` (95/95 across 9 files) — valid ingestion, deterministic normalization + IDs, first ingestion creates all / repeat is `already_ingested`, provenance round-trip, ground-truth isolation (source scan + rejected upload + no-leak), expected persisted counts, structured & user-safe errors, loaded investigation state. 3 Playwright tests (`ingestion.spec.ts` real empty→ingest→loaded + reload/idempotent; `shell.spec.ts` state-agnostic) — real local ingestion path, no mocked network, zero browser console errors. Explicit empty-DB→ingest→counts→re-ingest→idempotent run verified; no ground-truth rows in the DB path. `tsc --noEmit` clean; `eslint .` clean; `next build` succeeds; `git diff --check` clean; secret scan clean; no dependency changes. | Yes — the demonstrator can load the synthetic investigation end to end and see it loaded |
| M2 | Vertical Slice Walking Skeleton | PLANNED | Unassigned | — | Required at completion | Required at completion | No |
| M3 | Synthetic Evidence at Full Scale (Workstream A) | PARTIAL — the full Operation DarkNet Delhi corpus and its isolated ground truth exist and validate (P5.1, tasks A1–A5); hardening any pipeline stage against the corpus depends on M2/M4+ and remains PLANNED | Unassigned | `df60d05` (P5.1 corpus) | Required at completion | See P5.1 | No |
| M4 | Ingestion & Extraction Hardening (Workstream B) | PARTIAL — evidence ingestion (validate → normalize → deterministic IDs → provenance → idempotent persistence → loaded investigation state, task B1) and structured information extraction over all 12 evidence types (validate → parse → extract → validate → provenance → idempotent persistence, task B2) are implemented and covered by tests (P5.2, P5.3); task B3 (dedicated provenance/confidence/error-model hardening beyond what B1/B2 already enforce) remains PLANNED | Unassigned | `6795b5e` (P5.2 ingestion), `5c7fb9a` (P5.3 extraction) | See P5.2, P5.3 | See P5.2, P5.3 | No |
| P5.3 | Evidence extraction pipeline — structured information extraction (blueprint Workstream B / task B2), the second real investigation workflow after ingestion. A typed 7-stage extraction service (`src/lib/extraction/`: `select_evidence → parse_content → extract_facts → validate_records → attach_provenance → persistence → result`) returning a structured `ExtractionResult` (never throws for expected failures). Reads only already-ingested, already-persisted application evidence (never a file, never `evidence/ground-truth/`) and identifies facts explicitly stated by a single evidence item across all 12 supported evidence types (FIR, suspect/alias/phone/IMEI/vehicle/bank-account/location records, CDR events, financial transactions, witness statements, crime events) — entity mentions, attribute mentions, relationship mentions (source-local, e.g. "this record's alias field names X"), and event mentions (communications, transactions, crime events). Every `ExtractedRecord` now carries a `classification` field (domain-schema + DB-schema addition, migrated) fixed to `observed_fact` per `docs/requirements.md` §7, and full six-field provenance (`source`/`location`/`method`/`confidence`/`processingHistory`/`timestamp`) tracing to the exact evidence-item field it was read from (`<recordRef>#<fieldPath>`) — confidence reflects extraction quality only, never inflated for corroboration or volume. Deterministic content-addressed ids (`extracted_record_<hash>` from `evidenceItemId + fieldPath`) make extraction idempotent: first run creates all 1,996 records (99 entity / 60 attribute / 123 relationship / 1,714 event mentions over the 1,820-item corpus); repeat run `already_extracted`, 0 writes; a partial-write retry persists only the still-missing records. No entity resolution, relationship inference, graph construction, or investigative conclusion is performed anywhere in the extraction path — proved by tests against the real corpus's known ambiguous cases (the two structurally-distinct "Vikram Singh" mentions stay unmerged; alias-variant suspect records, including ones whose own `note` field literally says "same individual", stay independent; the FIR-vs-witness vehicle-colour contradiction survives unreconciled; the S1↔S4 hidden connection is never stated directly; mule-account/transaction facts are reproduced verbatim with no extraction-added suspicion label; low-value relationships get no materiality judgment). Ground-truth isolation: no `src/lib/extraction/` module imports the ground-truth loader or addresses `evidence/ground-truth/`. Structured, user-safe error taxonomy (`NO_INVESTIGATION`, `UNSUPPORTED_EVIDENCE_TYPE`, `VALIDATION_FAILURE`, `PERSISTENCE_FAILURE`, `INTERNAL_ERROR`). Streaming route handler `POST /api/extraction` (newline-delimited real stage events) + `GET` for `ExtractionState` + `GET /api/extraction/facts` (paginated, representative facts — never the full corpus in one response). UI: the evidence workspace gains an **Extract Evidence** action once an investigation is loaded — live 7-stage progress, a completion summary with counts by record type, and a focused, paginated extraction-results view showing each representative fact's type, observed value, source reference, provenance, confidence, and evidence classification; graph/timeline/map/analytics/Copilot/dossier nav stay disabled. `docs/data/extraction.md`. | Completed | Claude Code (AI) | `5c7fb9a` | `docs/progress/evidence/P5.3/` — `P5.3_screenshot-preextraction_2026-09-02_5c7fb9a.png` (investigation loaded, extraction available), `P5.3_screenshot-extracting_2026-09-02_5c7fb9a.png` (live 7-stage extraction), `P5.3_screenshot-extracted_2026-09-02_5c7fb9a.png` (extraction complete + record counts), `P5.3_screenshot-facts_2026-09-02_5c7fb9a.png` (representative extracted facts with provenance/source references). Captured by `CAPTURE_EVIDENCE=1 npx playwright test investigation-extraction.spec.ts` against the real running app. | 24 new Vitest tests in `tests/unit/extraction.test.ts` (119/119 across 10 files) — full-corpus extraction (all 7 stages, all 12 evidence types, all 4 record types), classification/confidence separation, provenance round-trip, deterministic ids, idempotency + partial-retry, ground-truth isolation (source scan + no-leak), structured & user-safe errors, and the non-inference proofs listed above. 2 new Playwright tests (`investigation-extraction.spec.ts` — real loaded-investigation→extract→completed→facts+provenance visible flow, and reload/re-extract idempotence; named to run after `ingestion.spec.ts` in the shared-DB e2e suite) — real local extraction path, no mocked network, zero browser console errors. Explicit empty-DB→ingest→extract→verify counts→verify provenance→verify classifications→re-extract→verify no duplicate records run verified. `tsc --noEmit` clean; `eslint .` clean; `next build` succeeds; `git diff --check` clean; secret scan clean; one migrated schema change (`extracted_records.classification`, additive column, no competing table). | Yes — the demonstrator can load the synthetic investigation, ingest it, and extract every explicit fact into a browsable, provenance-complete result |
| P5.4 | Entity resolution pipeline — deterministic identity clustering (blueprint Workstream C / tasks C1–C3), the third real investigation workflow after ingestion and extraction. A typed 8-stage resolution service (`src/lib/resolution/`: `select_records → canonicalize_identifiers → cluster_identities → resolve_mentions → validate_decisions → attach_provenance → persistence → result`) returning a structured `ResolutionResult` (never throws for expected failures). Reads only already-persisted extracted records (never a file, never `evidence/ground-truth/`). Two-tier deterministic evidence-only matching: Tier A canonicalizes phone/IMEI/vehicle/bank-account identifiers 1:1 and clusters person mentions that share an identifier stated by their own evidence item (a union-find over `has_phone`/`has_account`/`has_vehicle` relationship facts); Tier B resolves identifier-less mentions (FIR `accused`, witness `aboutNames`) by exact name-string match against exactly one Tier-A cluster — a match against two or more distinct clusters is left deliberately unmerged (`status: "ambiguous"`, confidence below the documented 0.5 merge floor, `candidateEntityIds` recorded, nothing dropped), proven via an adversarial synthetic fixture test since the real corpus contains no genuine identifier-anchored name collision (its one designed look-alike pair, two people named "Vikram Singh", never structurally collides because the bystander's name lives only in a witness statement's free text, which neither P5.3 extraction nor P5.4 resolution ever parses). New domain type `ResolutionDecision` (`src/lib/domain/resolution.ts`, new `resolution_decisions` table, migrated) kept deliberately distinct from the unchanged P4.2 `Entity`/`Alias` shapes — one decision per extracted record processed, `classification` fixed to `ai_inference` per `docs/requirements.md` §7 (entity resolution is that classification's own named example, however deterministic the rule), full six-field provenance whose `source` is the extracted record and whose `processingHistory` appends onto that record's own chain rather than replacing it. Deterministic content-addressed ids throughout make resolution idempotent: first run creates 54 entities (10 person + 44 identifier) + 25 aliases from 85 decisions; repeat run `already_resolved`, 0 writes; a partial-write retry persists only what's missing. No relationship between two different entities is ever created (`relationships` stays empty — graph synthesis's job); no entity attribute references another entity; contradictory source attributes are never touched or reconciled. Ground-truth isolation: no `src/lib/resolution/` module imports the ground-truth loader, addresses `evidence/ground-truth/`, or references any ground-truth-only field name. Structured, user-safe error taxonomy (`NO_INVESTIGATION`, `NO_EXTRACTED_RECORDS`, `VALIDATION_FAILURE`, `PERSISTENCE_FAILURE`, `INTERNAL_ERROR`). Streaming route handler `POST /api/resolution` + `GET` for `ResolutionState` + `GET /api/resolution/entities` (paginated) + `GET /api/resolution/entities/[id]` (full decision-chain detail). UI: the evidence workspace gains a **Resolve Entities** action once extraction is complete — live 8-stage progress, a completion summary with counts by entity kind, and a resolution-results view where every entity card expands to show its full **extracted fact → resolution decision → canonical entity** chain (source record, resolution type, reason, confidence, classification, conflicts); graph/timeline/map/analytics/Copilot/dossier nav stay disabled. `docs/data/resolution.md`. | Completed | Claude Code (AI) | `56cf883` | `docs/progress/evidence/P5.4/` — `P5.4_screenshot-preresolution_2026-09-02_56cf883.png` (extraction done, resolution available), `P5.4_screenshot-resolving_2026-09-02_56cf883.png` (live 8-stage resolution), `P5.4_screenshot-resolved_2026-09-02_56cf883.png` (resolution complete + entity counts), `P5.4_screenshot-nonmerge_2026-09-02_56cf883.png` (an isolated, unmerged mention's decision detail — a real non-merge outcome), `P5.4_screenshot-detail_2026-09-02_56cf883.png` (a merged entity's full evidence/provenance chain). Captured by `CAPTURE_EVIDENCE=1 npx playwright test investigation-resolution.spec.ts` against the real running app. | 26 new Vitest tests in `tests/unit/resolution.test.ts` (145/145 across 11 files) — full-corpus resolution (all 8 stages, entity/alias/decision counts, classification/confidence separation, provenance chain including processing-history append), deterministic ids, idempotency + partial-retry, ground-truth isolation (source scan + no-leak), structured & user-safe errors (including a hand-crafted malformed-candidate and a dangling-entity-reference case), and the non-inference proofs: one canonical "Vikram Singh" entity with exactly 5 contributing decisions, spelling-variant/nickname alias merges with preserved justification, contradictory attribute_mention records left untouched, zero relationship rows / no entity-to-entity references, cross-evidence identity support (one vehicle plate named in two different evidence items canonicalizes once), and an adversarial constructed ambiguous-name-conflict fixture proving the never-force-merge safeguard. 2 new Playwright tests (`investigation-resolution.spec.ts` — real extracted-investigation→resolve→completed→merge-and-non-merge-evidence-visible flow, and reload/re-resolve idempotence; named to run after `investigation-extraction.spec.ts` in the shared-DB e2e suite) — real local resolution path, no mocked network, zero browser console errors. Explicit empty-DB→ingest→extract→resolve→verify counts→verify provenance→verify classifications→re-resolve→verify no duplicate rows run verified manually. `tsc --noEmit` clean; `eslint .` clean; `next build` succeeds; `git diff --check` clean; secret scan clean; one migrated schema addition (`resolution_decisions`, a new table — `entities`/`aliases` reused unchanged, no competing table). | Yes — the demonstrator can load the synthetic investigation, ingest it, extract every explicit fact, and resolve those facts into a browsable, provenance-complete canonical-entity layer with merge decisions and ambiguity handling visible |
| P5.5 | Graph synthesis pipeline — deterministic edge construction (blueprint Workstream D / tasks D1–D3), the fourth real investigation workflow after ingestion, extraction, and entity resolution. A typed 10-stage graph service (`src/lib/graph/`: `load_resolved_entities → load_extracted_records → map_evidence_to_entities → construct_candidates → validate_endpoints → construct_edges → attach_provenance → persistence → build_in_memory_graph → result`) returning a structured `GraphResult` (never throws for expected failures). Reads only already-persisted resolved entities, extracted records, and locations (never a file, never `evidence/ground-truth/`) and populates the single table this milestone actually owns — `relationships` — never recreating identity resolution and never touching `locations`/`communication_events`/`financial_transactions`, which are already fully populated at P5.2 ingestion time from the corpus manifest (a real bug caught during manual verification: an earlier version mistakenly treated locations as its own to create, hashing them one part short of ingestion's own id scheme and silently doubling the location count from 14 to 28 — fixed by taking the already-persisted `Location[]` as an input and never constructing a new one, with `tests/unit/graph.test.ts` asserting the exact count as a regression guard). Endpoint resolution uses two deterministic paths only: a same-evidence-item sibling lookup (mirroring P5.4's own Tier-A clustering) and a bounded exact-match lookup against P5.4's already-resolved canonical registry — zero or multiple name matches are dropped with a warning, never guessed. Direct edges (`ownership`, `communication`, `financial`, `co_location`) classify `observed_fact`/`corroborated_fact` by contributing-evidence-item count; derived person↔person `communication`/`financial` edges (built only when both endpoints have a resolvable owning person) always classify `ai_inference`, confidence fixed at 0.7. Full corpus result: 54 canonical entities + 14 real locations = 68 nodes, 196 edges (38 ownership, 69 communication, 63 co_location, 26 financial; 156 corroborated_fact, 2 observed_fact, 38 ai_inference). The hidden S1↔S4 connection stays structurally indirect (no direct relationship row between Rohan Malhotra and Farhan Qureshi; only graph-traversal-reachable via the real financial chain and shared cell-tower co-location); the money-mule chain (Rohan Malhotra→Sunil Gupta→Pooja Rani→Ashok Kumar→Farhan Qureshi) is represented through real bank-account entities only — the mule intermediaries never receive a canonical person entity (P5.4 never created one, since they never appear in their own `suspect_record`), so no person-level edge is invented for those hops; three synthetic noise phone numbers correctly produce zero communication edges since they were never canonicalized as phone entities. Deterministic content-addressed relationship ids (`makeContentId("relationship", [type, source, target])`) make synthesis idempotent: first run creates every edge; repeat run `already_synthesized`, 0 writes; a partial-write retry persists only what's missing. Ground-truth isolation: no `src/lib/graph/` module imports the ground-truth loader, addresses `evidence/ground-truth/`, or references any ground-truth-only field name. Structured, user-safe error taxonomy (`NO_INVESTIGATION`, `NO_RESOLVED_ENTITIES`, `NO_EXTRACTED_RECORDS`, `VALIDATION_FAILURE`, `PERSISTENCE_FAILURE`, `INTERNAL_ERROR`). Streaming route handler `POST /api/graph` + `GET` for `GraphState` + `GET /api/graph/snapshot` (bounded, optionally neighborhood-focused) + `GET /api/graph/nodes/[id]` / `GET /api/graph/edges/[id]` (full detail, the evidence-trace answer). The in-memory graph (`src/lib/graph/runtime.ts`, `graphology`) is a deterministic, reconstructable projection of persisted state — rebuilt from SQLite, never the source of truth. UI: the sidebar's **Graph** nav entry, previously a permanently-disabled placeholder, becomes a real, clickable view that enables live once synthesis succeeds (no page reload); the evidence workspace gains a **Synthesize Graph** action once resolution is complete. The new Graph screen (sigma.js, sharing the same `graphology` model the server rebuilds) shows the bounded network with node-kind/edge-type filtering, a "jump to entity" picker (a deterministic selection affordance, not only canvas clicks), a focus-on-selection neighborhood view, a node-detail panel (label, aliases, attributes, provenance, every connected entity with a per-connection "inspect this relationship" affordance), and an edge-detail panel (type, direction, classification, confidence, attributes, conflicts, and the full source-`extracted_records` evidence trail). `docs/data/graph.md`. | Completed | Claude Code (AI) | `9a5fa41` | `docs/progress/evidence/P5.5/` — `P5.5_screenshot-pregraph_2026-09-02_9a5fa41.png` (resolution done, graph synthesis available), `P5.5_screenshot-synthesizing_2026-09-02_9a5fa41.png` (live 10-stage synthesis), `P5.5_screenshot-overview_2026-09-02_9a5fa41.png` (completed graph overview), `P5.5_screenshot-entity-neighborhood_2026-09-02_9a5fa41.png` (Rohan Malhotra selected with neighborhood), `P5.5_screenshot-relationship-provenance_2026-09-02_9a5fa41.png` (a selected ownership relationship's full evidence trail), `P5.5_screenshot-filtered-focused_2026-09-02_9a5fa41.png` (phone-kind filtered + focus-on-selection view). Captured by `CAPTURE_EVIDENCE=1 npx playwright test investigation-synthesis.spec.ts` against the real running app. | 35 new Vitest tests in `tests/unit/graph.test.ts` (183/183 across 11 files) — deterministic candidate-construction unit tests (ownership via sibling lookup and bounded name lookup, financial-chain non-invention, communication/co-location via the real Location bridge, unsupported/missing-endpoint handling), provenance/classification verification unit tests, idempotent-persistence unit tests, graph-reconstruction unit tests, and a full-corpus integration suite (all 10 stages, canonical-id-only endpoints, declared-type-only edges, complete provenance, deterministic ids + idempotent re-synthesis + partial retry, contradictory-evidence preservation, the S1↔S4 indirection proof via BFS, the money-mule chain proof via real account entities, noise-number rejection, Vikram Singh separation, source/evidence traceability, ground-truth isolation, graph-reconstruction determinism, and API-shaped query coherence). 2 new Playwright tests (`investigation-synthesis.spec.ts` — real resolved-investigation→synthesize→completed→live-nav-enable→entity-selection→relationship-inspection→filter/focus flow, and reload/re-synthesize idempotence; named to sort after `investigation-resolution.spec.ts` and before `shell.spec.ts` in the shared-DB e2e suite) — real local graph-synthesis path, no mocked network, zero browser console errors across 4 consecutive full-suite runs. `tsc --noEmit` clean; `eslint .` clean; `next build` succeeds; `git diff --check` clean; secret scan clean; one migrated schema change (`relationships` gains `directed`/`evidenceItemIds`/`extractedRecordIds`/`conflicts`/`attributes` columns, additive, no competing table); one new dependency (`sigma`, the stack contract's approved graph-visualization baseline). | Yes — the demonstrator can load the synthetic investigation, ingest it, extract every explicit fact, resolve those facts into canonical entities, and synthesize a browsable, provenance-complete investigative graph with node/edge selection and full evidence traceability |
| P5.6 | Topology analytics pipeline — deterministic structural signals over the P5.5 graph (blueprint Workstream E / tasks E1–E3), the fifth real investigation workflow after ingestion, extraction, resolution, and graph synthesis. A typed 10-stage analytics service (`src/lib/analytics/`: `load_graph_state → build_analysis_graph → compute_centrality → compute_bridges → compute_communities → compute_ranking → validate_signals → attach_provenance → persistence → result`) returning a structured `AnalyticsResult` (never throws for expected failures). Reads only already-persisted entities, locations, relationships, and the P5.5 graph-version marker (never a file, never `evidence/ground-truth/`) and populates the `analytical_signals` table the P4.2 foundation already provisioned — never touching `relationships`, `locations`, or any evidence/resolution table. Degree (total/weighted/in/out/by-relationship-type) is computed live on request rather than persisted, since it is a direct, cheap-to-recompute property of the already-persisted graph. Centrality (degree + betweenness, `graphology-metrics`) and communities (`graphology-communities-louvain`, seeded with a fixed deterministic PRNG since Louvain defaults to `Math.random`) use the stack contract's own approved analytics packages; bridge/articulation-point detection is a local, deterministic (sorted-neighbor-order) Tarjan's-algorithm implementation since graphology ships no built-in cut-vertex function. Community cluster ids are content-addressed from the sorted member-entity-id set — never the algorithm's raw integer index, which is not guaranteed stable between otherwise-identical runs — proven byte-identical across repeated full-signal-set synthesis runs. The investigative ranking combines betweenness (35%), degree centrality (35%), and a normalized bridge score (30%) with id-based tie-breaking, exposing every component metric alongside the combined score; it is explicitly labeled "structural prominence" / "investigative priority," never a "criminality score," and every bridge/community/ranking explanation states in plain language that the signal is never a claim of guilt or criminal involvement. Shortest-path queries are computed live and never persisted (a query-parameterized result, not a corpus-wide signal) via `graphology-shortest-path`'s `bidirectional` search over a deliberately undirected projection distinct from both the P5.5 rendering graph and the directed analytics graph — undirected because a strictly-directed search would report "no path" between two people connected only through an identifier one owns in the "always forward" `ownership` direction, which is investigatively wrong even though directionally accurate; each returned edge still reports its true stored direction. A missing endpoint, a same-entity query, or a genuinely disconnected pair (including under a relationship-type filter that excludes the only connecting edge type) all return a structured `{ found: false, reason }` result, never a thrown error. Full corpus result: 54 entities + 14 locations = 68 analyzed nodes, 196 edges, 136 centrality signals, 19 bridges, 11 communities, 68 ranked entities (234 total persisted signals). Key demonstrations verified against the real corpus (never hardcoded from ground truth): Rohan Malhotra and Kabir Sharma both rank among the top structurally prominent entities with real supporting edges; Vikram Singh remains exactly one canonical entity in ranking output; the unfiltered shortest path between Rohan Malhotra (S1) and Farhan Qureshi (S4) resolves as a real 2-hop path through Kabir Sharma (a directly-stated S3↔S4 communication pair plus an S3→S1 payment) — analytics never manufactures a direct edge — while a financial-only filtered query surfaces a separate real 3-hop derived chain (Rohan Malhotra→Anjali Verma→Deepak Yadav→Farhan Qureshi), distinct from the raw money-mule account chain (unreachable via this same person-level query since the mule intermediaries never received a P5.4 person entity); every financial community/edge stays represented through real bank-account entities only; every ranked phone entity has at least one real, persisted supporting relationship — no noise number gains fabricated connectivity. Every signal is classified exactly `algorithmic_signal` (docs/requirements.md §7), enforced at three layers (fixed-literal domain schema, `assertProvenance`'s explicit rejection of any other value, and a full serialized-output scan for every forbidden classification string) — never `observed_fact`/`corroborated_fact`/`ai_inference`/`investigative_lead`. Deterministic content-addressed signal ids include the graph version they were computed against, so re-synthesizing the graph naturally produces a fresh signal set rather than silently shadowing stale analytics; first run creates every signal, a repeat run against the same graph version writes nothing (`status: "already_synthesized"`), a partial-write retry persists only what's missing. Ground-truth isolation: no `src/lib/analytics/` module imports the ground-truth loader, addresses `evidence/ground-truth/`, or references any ground-truth-only field name — verified both by a source-code scan and by scanning the actual persisted signal output of a full-corpus run for the same forbidden strings. Structured, user-safe error taxonomy (`NO_INVESTIGATION`, `NO_GRAPH`, `VALIDATION_FAILURE`, `PERSISTENCE_FAILURE`, `INTERNAL_ERROR`). Streaming route handler `POST /api/analytics` + `GET` for `AnalyticsState` + `GET /api/analytics/entities` (paginated ranking) + `GET /api/analytics/entities/[id]` (degree, every targeting signal with its supporting edges, community membership) + `GET /api/analytics/bridges` + `GET /api/analytics/communities` + `GET /api/analytics/path` (relationship-type-filterable shortest path). UI: the sidebar gains a real, clickable **Analytics** entry that enables live once synthesis succeeds (mirroring the P5.5 Graph entry, no page reload); the evidence workspace gains a **Run Analytics** action once graph synthesis is complete. The new Analytics screen shows an overview strip, a tab switcher between ranked/bridge/community views, an entity-detail panel, and a shortest-path panel with relationship-type filters — every UI surface labels its output Algorithmic Signal with explicit never-a-claim-of-guilt language, and selecting an entity or path result offers **View in graph**, which cross-navigates to the Graph screen focused on that entity's real neighborhood (`GraphScreen` gains an `initialFocusNodeId` prop) so Analytics and Graph read as one connected investigative surface. `docs/data/analytics.md`. | Completed | Claude Code (AI) | `f1fb48e` | `docs/progress/evidence/P5.6/` — `P5.6_screenshot-preanalytics_2026-09-02.png` (graph synthesized, analytics available), `P5.6_screenshot-overview_2026-09-02.png` (analytics overview + ranked entities), `P5.6_screenshot-ranked_2026-09-02.png` (full ranked-entities list, 68 of 68), `P5.6_screenshot-entity-detail_2026-09-02.png` (a ranked entity's degree breakdown + algorithmic signals + community membership), `P5.6_screenshot-bridges_2026-09-02.png` (structural bridge entities), `P5.6_screenshot-communities_2026-09-02.png` (community/cluster view), `P5.6_screenshot-shortest-path_2026-09-02.png` (Rohan Malhotra → Kabir Sharma → Farhan Qureshi, the real S1↔S4 path), `P5.6_screenshot-path-filtered_2026-09-02.png` (the same query financial-only filtered, surfacing the separate Rohan→Anjali→Deepak→Farhan chain). Captured by `CAPTURE_EVIDENCE=1 npx playwright test investigation-topology.spec.ts` against the real running app. | New Vitest tests in `tests/unit/analytics.test.ts` (240/240 across 12 files) — deterministic build-algorithm unit tests (degree breakdown, articulation-point detection on a hand-built barbell graph, centrality bounds, content-addressed community determinism, ranking permutation + tie-breaking + repeated-invocation determinism, shortest-path on a barbell graph including the ownership-direction edge case and relationship-type filtering), provenance/classification verification unit tests (including the location-endpoint acceptance case), idempotent-persistence unit tests, ground-truth isolation (source scan), and a 30+-test full-corpus integration suite (all 10 stages, degree/centrality/bridge/community correctness against real persisted data, deterministic idempotent re-synthesis, ranking permutation integrity, filtered/unfiltered shortest paths with path provenance, structured no-path/malformed-query results, classification correctness, signal provenance with id-only references, ground-truth isolation over live persisted output, empty-database and no-graph-yet structured errors, and every required investigative demonstration). 2 new Playwright tests (`investigation-topology.spec.ts` — real graph-synthesized→analytics→ranked/bridges/communities/entity-detail/shortest-path/filtered-path/cross-navigation-to-graph flow, and reload/re-synthesize idempotence; named to sort after `investigation-synthesis.spec.ts` and before `shell.spec.ts` in the shared-DB e2e suite) — real local analytics path, no mocked network, zero browser console errors across 3 consecutive full-suite runs. `tsc --noEmit` clean; `eslint .` clean; `next build` succeeds; `git diff --check` clean; secret scan clean; one migrated schema change (`analytical_signals.target_entity_id` FK relaxed to unconstrained, since the analysis graph includes both entities and locations as valid targets — a real bug this milestone's own full-corpus smoke test caught, a `FOREIGN KEY constraint failed` on the first location-targeted signal insert); three new dependencies (`graphology-metrics`, `graphology-communities-louvain`, `graphology-shortest-path` — the stack contract's own approved graph-analytics baseline, none newly introduced). | Yes — the demonstrator can load the synthetic investigation, ingest it, extract every explicit fact, resolve those facts into canonical entities, synthesize a browsable investigative graph, and now compute and explore deterministic, fully-cited structural analytics (centrality, bridges, communities, ranking, shortest path) with a working cross-navigation link back to the graph |
| M5 | Entity Resolution (Workstream C) | PARTIAL — deterministic identity clustering (shared-identifier + exact-name matching, ambiguity detection, provenance-complete resolved entities) is implemented and covered by tests (P5.4, tasks C1–C3); the confidence floor is documented (`MERGE_CONFIDENCE_FLOOR = 0.5`, `src/lib/domain/resolution.ts`) but a precision/recall measurement against ground truth (per `docs/evaluation/evaluation-spec.md`, `TO BE DEFINED BEFORE IMPLEMENTATION`) remains PLANNED, as does a dedicated human review queue UI for ambiguous candidates (currently surfaced inline in the resolution-results view, not a separate triage workflow) | Unassigned | `56cf883` (P5.4 resolution) | See P5.4 | See P5.4 | No |
| M6 | Graph Synthesis (Workstream D) | PARTIAL — deterministic edge construction (ownership/communication/financial/co_location, plus derived person↔person edges) into the `relationships` table is implemented and covered by tests (P5.5, tasks D1–D3), with a working graph-exploration API (snapshot/node-detail/edge-detail) and UI; topology analytics (centrality/community/path — task set for M7) remain PLANNED, as this milestone deliberately excludes them | Unassigned | `9a5fa41` (P5.5 graph synthesis) | See P5.5 | See P5.5 | Yes — graph visualization and exploration are demo-ready |
| P5.7 | Spatial & temporal corroboration pipeline — deterministic spatial/temporal correlation over persisted evidence (blueprint Workstream F / tasks F1–F3), the sixth real investigation workflow after ingestion, extraction, resolution, graph synthesis, and topology analytics. A typed 10-stage corroboration service (`src/lib/corroboration/`: `load_graph_state → build_activity_index → compute_spatial → compute_temporal → compute_spatiotemporal → classify_findings → validate_findings → attach_provenance → persistence → result`) returning a structured `CorroborationResult` (never throws for expected failures). Reads only already-persisted observable state — P5.2 `communication_events`, P5.3 `event_mention` records (financial), P5.4 resolved entities, P5.5 `relationships` (ownership edges roll a phone/account up to its owning person), P5.2 `locations`, `evidence_items` — plus the P5.5 graph-version marker; never a file, never `evidence/ground-truth/`, never an invented coordinate or timestamp. Populates one new migrated table, `corroboration_findings`. Five deterministic finding types: `spatial_co_location` (two subjects each with ≥2 activity events at the same persisted location), `spatial_proximity` (two distinct case locations within the documented 1 000 m haversine threshold — the stack contract's approved approach, no PostGIS — at least one with recorded activity; always an algorithmic signal), `temporal_co_occurrence` (two subjects active within the documented 30-minute window, contributed by ≥2 distinct evidence items — a single joint record is not a corroboration signal and is not emitted), `repeated_spatiotemporal_overlap` (a subject pair active at the same location within the window on ≥2 separate occasions), and `spatiotemporal_contradiction` (one subject placed at two locations implying a travel speed above the documented 55 m/s ≈ 198 km/h ceiling — both source records cited, neither presumed correct; always an algorithmic signal). Classification is restricted to exactly two of `docs/requirements.md` §7's five values — `corroborated_fact` (spatial/temporal co-occurrence independently attested by ≥2 distinct evidence items — §7's "…or by spatial/temporal corroboration") or `algorithmic_signal` (algorithmically-derived pattern) — enforced at three layers (a two-value domain enum with refinements that a corroborated_fact cites ≥2 evidence items and a contradiction is always an algorithmic_signal, `verify.ts`'s `assertProvenance` re-check, and a serialized forbidden-classification scan); `observed_fact`/`ai_inference`/`investigative_lead` are never valid. Every UI surface and every persisted `explanation` string states in plain language that a finding is never a claim that two entities were together or that timing implies contact/causation, and a contradiction is reported as a conflict, never silently resolved. Deterministic content-addressed finding ids include the graph version, so a re-synthesized graph produces a fresh finding set; first run creates every finding, a repeat run against the same graph version writes nothing (`status: "already_synthesized"`), a partial-write retry persists only what's missing. Full corpus result: 3,332 observable activity events across 15 subjects and 8 located sites → 456 findings — 438 corroborated facts (260 co-location + 102 temporal + 76 repeated overlap; the synthetic network operates tightly within a few Delhi sectors, so its dense CDR stream genuinely places most active pairs at a shared tower across ≥2 independent call records — corroboration by §7's definition) and 18 algorithmic signals (3 proximity — e.g. the Karol Bagh warehouse crime scene 979 m from an active tower; 3 single-occasion temporal; 12 travel-speed contradictions surfaced from the raw CDR stream without consulting any answer key). Every finding carries full six-field provenance (`method` = `corroboration:<algorithm>`, `location` = `graph_version:<v>`, `confidence` = 1 for a deterministic computation with strength carried by `value.distinctEvidenceItemCount`/`occurrenceCount`, `processingHistory` tracing back through the graph version) plus `evidenceItemIds` (every distinct source `evidence_items.id` compared) and `supportingRecordIds` (the `communication_events.id`/`extracted_records.id` values compared); `value` references ids only, never an inline evidence copy; `verify.ts` asserts every cited evidence-item id and every entity/location endpoint resolves to a real persisted row. Ground-truth isolation: no `src/lib/corroboration/` module imports the ground-truth loader or the corpus case-design module, addresses `evidence/ground-truth/`, or references any ground-truth-only field name — verified by a source-code scan and by scanning the actual persisted finding output of a full-corpus run for the same forbidden strings. Structured, user-safe error taxonomy (`NO_INVESTIGATION`, `NO_GRAPH`, `INSUFFICIENT_SPATIAL_TEMPORAL_DATA` — reported distinctly from "checked, nothing found", per Agent 5's contract — `VALIDATION_FAILURE`, `PERSISTENCE_FAILURE`, `INTERNAL_ERROR`). Streaming route handler `POST /api/corroboration` + `GET` for `CorroborationState` + `GET /api/corroboration/findings` (paginated, `?kind`/`?type`/`?classification`/`?entityId` filters) + `GET /api/corroboration/findings/[id]` (one finding's full detail + provenance) + `GET /api/corroboration/pairs` (entity-pair overlap roll-up, strongest-corroboration first). UI: the sidebar gains a real, clickable **Corroboration** entry that enables live once synthesis succeeds (mirroring the P5.5/P5.6 entries, no page reload); the evidence workspace gains a **Run Corroboration** action once analytics is complete. The new Corroboration screen is an investigator workspace — an overview strip, a corroborated-fact / algorithmic-signal filter, tabs for Entity pairs / Spatial / Temporal / Repeated overlaps / Contradictions, a deterministic timeline for the temporal views, side-by-side conflicting placements for contradictions, and a detail panel that always shows the finding's classification, the structured metric that produced it, the full provenance chain, and the exact source evidence-item / observable-record ids; selecting an entity pair cross-filters every tab, and **View in graph** hands off to the Graph screen focused on that entity's neighborhood. `docs/data/corroboration.md`. | Completed | Claude Code (AI) | `6d2ae7c` | `docs/progress/evidence/P5.7/` — `P5.7_screenshot-precorroboration_2026-09-03.png` (analytics synthesized, corroboration available, nav disabled), `P5.7_screenshot-running_2026-09-03.png` (the live 10-stage stream), `P5.7_screenshot-completed-summary_2026-09-03.png` (spatial/temporal/overlap/contradiction counts + the corroborated-fact vs algorithmic-signal split), `P5.7_screenshot-spatial_2026-09-03.png` (the spatial findings list), `P5.7_screenshot-temporal_2026-09-03.png` (the temporal timeline), `P5.7_screenshot-detail-provenance_2026-09-03.png` (a finding's classification, metric, full provenance chain and cited evidence-item ids), `P5.7_screenshot-filtered_2026-09-03.png` (the algorithmic-signal filter narrowing the result), `P5.7_screenshot-rerun-idempotency_2026-09-03.png` (reload → synthesized state persists, re-run reports already complete). Captured by `CAPTURE_EVIDENCE=1 npx playwright test investigation-zz-corroboration.spec.ts` against the real running app. | New Vitest tests in `tests/unit/corroboration.test.ts` (299/299 across 14 files) — deterministic primitive unit tests (haversine known distances + symmetry + integer rounding, window/threshold inclusive boundaries, implied-speed zero-time handling), per-computation behaviour + adversarial false-match tests (same-place-far-apart-in-time is not a repeated overlap; close-in-time-different-far-locations is not a co-location; a single shared CDR is not a temporal corroboration; a plausible 2-hour move is not a contradiction), classification-logic tests, determinism / input-row-order independence, graph-version id isolation, provenance/endpoint verification invariants, idempotent partial-retry persistence, ground-truth source scan, and a full-corpus integration suite (all 10 stages, classification/provenance/endpoint invariants over persisted output, deterministic idempotent re-synthesis + partial retry, filter coherence, entity-pair roll-up integrity, ground-truth isolation over live persisted output, `NO_GRAPH`/`NO_INVESTIGATION` structured errors). 2 new Playwright tests (`investigation-zz-corroboration.spec.ts` — real analytics-synthesized→corroboration→pairs/spatial-detail+provenance/timeline/contradictions/classification-filter/cross-navigation-to-graph flow, and reload/re-synthesize idempotence; named to sort after every other `investigation-*` spec in the shared-DB e2e suite since corroboration depends on all of them) — real local corroboration path, no mocked network, zero browser console errors; full e2e suite 13/13. `tsc --noEmit` clean; `eslint .` clean; `next build` succeeds; `git diff --check` clean; secret scan clean; one migrated schema addition (`corroboration_findings`, a new table — no competing representation, every read/write through the validated repository layer); no new dependencies. | Yes — the demonstrator can load the synthetic investigation, ingest it, extract every explicit fact, resolve those facts into canonical entities, synthesize a browsable investigative graph, compute structural analytics, and now corroborate spatially and temporally — where entities were active, what shared a location or a 30-minute window, which pairs repeatedly overlapped, and which placements are physically impossible — every finding classified corroborated fact or algorithmic signal, fully cited, with a working cross-navigation link back to the graph |
| M7 | Network Analytics & Spatial/Temporal Corroboration (Workstreams E, F) | PARTIAL — deterministic topology analytics (degree, degree/betweenness centrality, bridge/articulation-point detection, Louvain community detection, an investigative-prominence ranking, and relationship-type-filterable shortest-path queries) is implemented and covered by tests, with a working analytics API and UI cross-navigating to the graph (P5.6, tasks E1–E3); deterministic spatial & temporal corroboration (co-location, haversine proximity, shared-time-window co-occurrence, repeated spatiotemporal overlap, travel-speed contradiction detection) is implemented and covered by tests, with a working corroboration API and an investigator-facing corroboration screen (P5.7, tasks F1–F3). Both workstreams E and F are now implemented; contradiction precision/recall measurement against ground truth (per `docs/evaluation/evaluation-spec.md`) remains a later M12 concern | Unassigned | `f1fb48e` (P5.6 topology analytics), `6d2ae7c` (P5.7 spatial/temporal corroboration) | See P5.6, P5.7 | See P5.6, P5.7 | Yes — structural analytics and spatial/temporal corroboration exploration are demo-ready |
| P5.8 | Investigation Copilot — grounded question answering (blueprint Workstream G / tasks G1 grounded retrieval, G2 grounded answer generation, G4 anti-hallucination guardrail), the seventh real investigation workflow. A typed 9-stage Copilot service (`src/lib/copilot/`: `parse_question → ground_entities → retrieve_evidence → assemble_pack → build_claims → synthesize_answer → validate_response → verify_citations → result`) returning a structured `CopilotResult` (never throws for expected failures). The load-bearing decision is that **a model never contributes a fact**: every claim an answer can make is built in deterministic TypeScript (`retrieval.ts`) from an already-persisted record — P5.2 evidence items / communication events / locations, P5.3 extracted records, P5.4 resolved entities / aliases / resolution decisions, P5.5 relationships, P5.6 analytical signals, P5.7 corroboration findings — and carries that record's OWN evidence classification and confidence forward unchanged (`corroborated_fact > observed_fact > algorithmic_signal > ai_inference > investigative_lead`, answer-level label fixed by schema refinement to the weakest cited claim). Two deliberately-distinct Zod schemas: `ModelAnswerSchema` (the only thing a model may emit — prose + pack-local citation handles `C3`/`EV12`, no ids, classifications, confidences or provenance) and `CopilotResponseSchema` (the validated response — per-claim classification, per-claim confidence, citations resolved into persisted ids, six-field provenance + a derivation record). The **anti-hallucination guardrail** (`verify.ts`, blueprint G4 — an automated output-boundary check, not a prompt instruction) runs at three stages: `enforceClassifications` before any model call (a fact claim must cite a fact-classified evidential record; a `corroborated_fact` needs ≥2 distinct evidence items or a corroborated finding; a claim citing only an entity can never be a fact), `validateModelAnswer` on the model's raw output (schema, unknown handles, inline-citation presence, fabricated identifier/name/date/phone literals, unsupported contact/causation/certainty phrasing — on the body and every caveat), and `assertCitationsResolve` on the final response (every cited id must resolve against the live store). Any failure discards the model wording whole and serves the deterministic narration (`narrate.ts`, a pure function of the claim set) with the rejection recorded; a cache hit is re-validated through the same guardrails. The LLM response cache (`src/lib/ai/cache.ts`) is keyed on a composite of model (`claude-opus-5`), prompt version (`copilot.system.v1`), output-schema version (`copilot.answer.v1`), normalized input, and generation config (`maxTokens 1500, temperature 0, effort medium`) — a model swap, prompt edit or schema tightening must miss. Ambiguity (a surface matching >1 entity) short-circuits to `status: "ambiguous"` exposing the candidates with `retrieve_evidence`/`synthesize_answer` reported skipped, never guessed; an unmatched subject returns `insufficient_evidence` naming the reference; conflicts are reported and never resolved. Ground-truth isolation: no `src/lib/copilot/` or `src/lib/ai/` module imports the ground-truth loader, addresses `evidence/ground-truth/`, or references a ground-truth-only field name — verified by a source-code scan and by scanning a full-corpus run's claim set + narration. With no `AI_PROVIDER_API_KEY` (the repo default, and the state every test and screenshot was produced in) the model is never called; grounding/citations/classifications/confidences are identical and only the prose degrades, disclosed in the response and UI. Structured, user-safe error taxonomy (`NO_INVESTIGATION`, `NO_DERIVED_INTELLIGENCE`, `INVALID_QUESTION`, `RETRIEVAL_FAILURE`, `VALIDATION_FAILURE`, `INTERNAL_ERROR`; model problems surfaced separately as `MODEL_NOT_CONFIGURED`/`MODEL_REQUEST_FAILED`/`MODEL_OUTPUT_REJECTED`, never a service failure). Streaming route `POST /api/copilot` (newline-delimited real stage events) + `GET /api/copilot` (`CopilotState` — readiness, corpus counts, model/prompt/schema versions, the eight bound suggested questions). UI: the sidebar's "Ask a Question" screen — enabled live once corroboration completes; a command bar with the eight canonical demo-contract questions bound from persisted data (q2/q3/q7 placeholders bound from the graph, never from ground truth); the real nine-stage stream; an answer in fixed reading order (question → answer → classification/confidence/grounding → supporting evidence → provenance/derivation → related graph/analytics/corroboration); every claim expands to its own classification, confidence, explanation and cited record ids; "Open in graph/analytics/corroboration" cross-navigation. Full corpus (1,820 evidence items, 1,996 extracted records, 54 entities, 25 aliases, 196 relationships, 234 analytical signals, 456 corroboration findings): all 8 canonical questions answer with ~65 grounded claims across ~225 cited records — q1/q3/q5/q8 reproduce the ground-truth narrative exactly (q1 all 8 suspects + aliases as Observed Fact; q3 the exact `SYN-AC-000001 → SYN-MA-000001 → SYN-MA-000002 → SYN-MA-000003 → SYN-SH-000001` mule chain; q5 the W3/W7 Vikram Singh contradiction as an unresolved lead; q8 the corroborated-fact/AI-inference/algorithmic-signal split), and q2/q4/q6/q7 are fully grounded, cited and correctly classified but grounded-divergent from the ground-truth narrative for documented reasons (shortest-path vs a hand-picked connector; co-location findings surfaced beside crime events without a join; composite ranking vs pure betweenness; intermediary retrieval gated on a persisted bridge signal, with q7's bound subject also non-deterministic across graph re-syntheses) — reconciliation is M12's job. G3 (multi-turn follow-up) is **not** implemented; `docs/implementation-blueprint.md` §6 lists it as a documented scope-cut. Documented in `docs/data/copilot.md`. | Completed | Claude Code (AI) | `Pending` | [docs/progress/evidence/P5.8/](evidence/P5.8/) — 11 screenshots + README (initial bound questions, grounded answer with per-claim classification, nine-stage stream, claim citations, provenance/derivation, ambiguity, insufficient evidence, contradiction, three-question session), captured against the real running app with no AI key | 105 unit tests (`tests/unit/copilot.test.ts`) + 4 real-app E2E tests (`tests/e2e/investigation-zzz-copilot.spec.ts`) | Yes |
| P5.9 | Dossier / report generation — the investigator-facing case report (blueprint Workstream H / tasks H1–H3), the seventh and final investigation workflow of the core user journey. A typed 11-stage dossier service (`src/lib/dossier/`: `load_case_state → assemble_summary → assemble_entities → assemble_signals → assemble_contradictions → collect_copilot → compose_report → validate_report → verify_traceability → persistence → result`) returning a structured `DossierResult` (never throws for expected failures). The dossier is an **assembly, never an analysis**: every substantive finding is read off a row an earlier stage already persisted — P5.2 evidence sources/items and locations, P5.3 extracted records, P5.4 entities/aliases/resolution decisions, P5.5 relationships, P5.6 analytical signals, P5.7 corroboration findings, P5.2 communication events — and carries that row's OWN classification and confidence forward unchanged. It reads only persisted state through the validated repository plus the existing P5.8 Copilot service; never a file, never an upload, never `evidence/ground-truth/`. Populates one new migrated table, `dossiers`. **Twelve sections** (H1 content model, expressed as Zod schemas rather than prose): case summary, evidence inventory, key entities, key relationships, analytical signals, spatial & temporal corroboration, contradictions, investigative leads & human verification, Copilot-supported material, provenance & traceability, classification & confidence, limitations & non-conclusions. **Classification is preserved structurally, not by convention**: `SECTION_ALLOWED_CLASSIFICATIONS` fixes which of the five §7 labels each section may carry, as a schema refinement — a contradiction cannot be stored as anything but an Algorithmic Signal, a lead cannot be stored as a fact, an entity cannot be stored above AI Inference, and `syntheticDataOnly`/`humanVerificationRequired` are `true` literals a caller cannot switch off. Statement wording follows classification: established-fact phrasing is reserved for Observed and Corroborated Facts, and that is asserted by test. **Traceability is enforced before any write** (H2's fail-loudly requirement): every finding must be classified with a label its section permits, cite at least one persisted record, have every cited id resolve against the live store, and carry complete six-field provenance ending at `dossier:assemble` with the upstream chain preserved. Any failure aborts the whole report and names the offending findings. This caught two real modelling errors on the first full-corpus run (location ids filed as entity ids; communication-event ids filed as extracted-record ids), now fixed by partitioning ids on their deterministic `kind_` prefix. **Deterministic and idempotent**: assembly is a pure function of the persisted snapshot plus a caller-supplied `generatedAt`; identity is a SHA-256 digest of the report body excluding timestamps and Copilot wording, so regeneration over unchanged state reuses the existing row (`already_generated`, `{created:0, skipped:1}`) and keeps the original generation time, while a new graph version yields a new report and marks the previous one stale rather than overwriting it. **No live AI request is required**: with no `AI_PROVIDER_API_KEY` the deterministic sections are unaffected and Copilot excerpts use the existing deterministic narration of the same grounded, guardrail-checked claim set, labelled as such on every excerpt, in the report's `aiSynthesisNote`, and in the limitations — no fake AI output is generated, and an unconsultable question is recorded as an explicit gap with its reason. API: `POST /api/dossier` (streamed 11-stage ndjson), `GET /api/dossier` (state: not_available/pending/generated/stale), `GET /api/dossier/report` (full report + resolved reference labels + stale flag). UI: the real Dossier screen in the investigation shell, preserving classification and traceability visibly per H3 — every finding shows its own classification badge and confidence inline and expands to its explanation, the persisted ids it rests on, its full provenance chain, and cross-navigation into Evidence/Graph/Analytics/Corroboration. Reload preserves the report (it is persisted and re-read on mount); regeneration is idempotent and says so. Full corpus: 12 sections, 104 findings (12 Observed Fact, 33 Corroborated Fact, 34 Algorithmic Signal, 13 AI Inference, 12 Investigative Lead), 12 contradictions, 12 leads, 3 Copilot excerpts, 1,311 resolved references. Documented in `docs/data/dossier.md`. | Completed | Claude Code (AI) | `d593074` | [docs/progress/evidence/P5.9/](evidence/P5.9/) — 11 screenshots plus the required side-by-side comparison, captured at `165777f` | 58 unit tests (`tests/unit/dossier.test.ts`) + 3 real-app E2E tests (`tests/e2e/investigation-zzz-dossier.spec.ts`) | Yes |
| P5.10.2 | Command-centre visual system + shell (blueprint Workstream I / M10, first UI increment) — a re-skin and re-frame only: no backend, API, domain, agent-contract, Copilot (M8) or Dossier (M9) behaviour changed, and no new dependency. **Dark operational visual system** (`src/app/globals.css`): the operational palette — `surface-1/2/3`, `border` / `border-strong`, `fg` / `fg-muted` / `fg-faint`, `accent` / `accent-quiet` — now lives on bare `:root`, so a browser with no colour-scheme preference renders the operational theme; a coherent light variant is retained under `prefers-color-scheme: light` only. Adds a graph node-kind palette, a relationship-type palette, and five evidence-classification token triplets. Every `--color-*` name the rest of the app already consumes as a Tailwind utility (`bg-card`, `text-muted-foreground`, `border-border`, `bg-accent`, …) is bridged to the new tokens, so existing class usage renders unchanged. **Shared primitives** (`src/components/ui/`): `ClassificationChip` (the canonical five-label chip — each of the §7 classifications now carries its own fill / text / border-style / glyph, ending the old treatment that gave Algorithmic Signal, AI Inference and Investigative Lead an identical outline), `ProvenanceBlock` (the six §8 provenance fields as a mono key/value grid with an audit-trail border; `confidence` optional, for the five-field graph provenance), `EmptyState` / `LoadingState` / `ErrorState`, and `Panel` (hero / panel / inset density tiers — successor to the one-size `Card`, which stays exported for un-migrated surfaces). **Command-centre shell**: `header.tsx` is now a command bar carrying the product identity, the loaded case's name + corpus version, the live 9-stage pipeline meter (real `completedStages`, relocated from the workspace, keeping its `Investigation pipeline status` accessible name), a dismissible focused-entity chip fed by the existing cross-navigation callbacks, and the non-removable synthetic-data marker; `sidebar.tsx` is a collapsible navigation rail (`Case / Analysis / Assist / Report`, preference persisted per browser via `useSyncExternalStore` with no hydration mismatch, accessible names stable in both states, `Overview` added as a disabled stub, `Timeline` / `Map` still disabled with no backing stage); `app-shell.tsx` lifts a `focusedEntityId` for the chip and frames the fixed command bar / rail / scrolling workspace. **Minimal primitive integration, no behaviour change**: `ClassificationChip` replaces the `badgeVariant` classification badges in `copilot-answer.tsx`, `dossier-report.tsx`, `corroboration-screen.tsx`, `graph-node-detail.tsx` and `graph-edge-detail.tsx`; `ProvenanceBlock` replaces the six-field grid on the dossier finding and the one-line provenance on the graph node/edge panels. Every `data-testid`, ARIA name/role, navigation callback and route/state behaviour is preserved; no unit or e2e test changed. Validation on this commit: `tsc --noEmit` clean, `eslint .` clean, `vitest run` 462/462, `next build` ok, `playwright test` 20/20, `git diff --check` clean. M10 remains open; graph redesign, the Map and Timeline surfaces, and broader state-primitive adoption are M10.3+. | In Progress | Claude Code (AI) | `Pending` | [docs/progress/evidence/P5.10.2/](evidence/P5.10.2/) — 10 dark-theme screenshots + a pre/post shell comparison + an mp4 recording, captured against the e2e corpus by `capture.mjs` | 462 unit tests + 20 e2e specs, all unchanged and green (no new automated tests this increment; the primitives are exercised through the surfaces that adopt them) | No |
| P5.10.3 | Shared investigation Inspector + persistent focused entity (blueprint Workstream I / M10, second UI increment) — a UI composition change only: no backend, API, domain, agent-contract, Copilot (M8) or Dossier (M9) behaviour changed, and no new dependency. **One right-hand Inspector** (`src/components/investigation/inspector/`) is now shared by the Graph, Analytics and Corroboration screens. It renders exactly one of: **`EntityProfile`** — the merged entity surface (identity and kind, aliases, attributes, the structural metrics / community / algorithmic signals from `/api/analytics/entities/{id}`, six-field provenance via `ProvenanceBlock`, and every connected entity as a re-focus target), fetched from `/api/graph/nodes/{id}` + `/api/analytics/entities/{id}` and showing whatever came back (the Graph screen is reachable before analytics has run); **`RelationshipDetail`** — the former edge panel, its source-evidence rows now opening the reference mode; **`FindingDetail`** — the former corroboration finding panel, its supporting-evidence chips now opening the reference mode; **`EvidenceReference`** — a lightweight view of what the citing surface knew about an evidence id, explicit that the full source record is M10.6; or an empty state. **Persistent focused entity**: `app-shell.tsx` collapses the three per-screen focus ids into one `focusedEntityId`. Cross-navigation sets it, selecting an entity in any surface sets it, it survives moving between Graph / Analytics / Corroboration (each screen reconciles the prop render-phase, no keyed remount), and the command-bar focus chip clears it everywhere. **Cross-navigation both ways**: every EntityProfile carries Graph / Analytics / Corroboration buttons; the Graph screen now also receives the shell's `viewIn*` callbacks. `graph-node-detail.tsx`, `graph-edge-detail.tsx` and `analytics-entity-detail.tsx` are **removed** — their behaviour and every `data-testid` they carried (`graph-node-detail`, `graph-node-label`, `graph-node-connection`, `graph-node-connection-inspect`, `graph-edge-detail`, `graph-edge-classification`, `graph-edge-evidence-item`, `analytics-entity-detail`, `analytics-entity-label`, `analytics-entity-signal`, `analytics-view-in-graph`, `corroboration-detail*`, `corroboration-view-in-graph`, …) are preserved by the new components (context-mapped in `EntityProfile`). Every ARIA name/role, navigation callback and route/state behaviour preserved; the 20 pre-existing e2e specs are unchanged. Validation on this commit: `tsc --noEmit` clean, `eslint .` clean, `vitest run` 462/462, `next build` ok, `playwright test` 21/21 (the 20 existing + `tests/e2e/investigation-zzz-inspector.spec.ts` proving one shared profile across Analytics and Graph, persistent focus, explicit clear, and every Inspector mode), `git diff --check` clean. M10 remains open; the graph-canvas redesign, the Map and Timeline surfaces, and the rich Evidence surface are M10.4+. | In Progress | Claude Code (AI) | `Pending` | [docs/progress/evidence/P5.10.3/](evidence/P5.10.3/) — 8 dark-theme screenshots (the same profile from Analytics and Graph, focus persistence, relationship / finding / evidence-reference modes, cleared Inspector) + an mp4 cross-navigation recording, captured against the e2e corpus by `capture.mjs` | 462 unit tests (unchanged) + 21 e2e specs (20 unchanged + 1 new: `investigation-zzz-inspector.spec.ts`) | No |
| P5.10.4 | Graph surface redesign (blueprint Workstream I / M10, third UI increment) — a presentation-only change: no backend, API, domain, graph-algorithm, agent-contract, Copilot (M8) or Dossier (M9) behaviour changed, graph data unchanged, no new dependency, and Sigma.js is not replaced. **Deterministic spatialization** (`src/lib/graph/layout.ts`): the circular/index-based node placement in `graph-view.tsx` is replaced with a self-contained, ForceAtlas2-style force-directed simulation (mass-scaled repulsion, spring attraction along real edges only, constant-magnitude FA2-default gravity, a linear cooling schedule) — no `graphology-layout-forceatlas2` or any other layout package. Positions are seeded per node id (an FNV-1a hash into a `mulberry32` PRNG), never from array order or `Math.random`, computed once per snapshot load and handed to sigma as static coordinates — never recomputed per frame. Verified deterministic by direct test (`tests/unit/graph-layout.test.ts`) and, at the app level, by re-screenshotting the full graph overview after a hard reload and diffing the PNG bytes: identical (`sha1 fdbbf2e93a52345797b0537ae32aabbb9d7f1054` both times — see the P5.10.4 evidence README). **Canvas fills the workspace**: the 520px fixed-height canvas is gone; `graph-screen.tsx`'s root is now `h-full` inside the shell's scrolling workspace, with the canvas+Inspector row taking `flex-1 min-h-0` — no page-level height cap, Inspector layout unchanged. **Encodings read from the M10.2 tokens, never a second palette** (`src/lib/graph/tokens.ts`): node fill and edge stroke resolve the existing `--kind-*` / `--edge-*` custom properties from `globals.css` at runtime via `resolveToken` (client-only, cached). This surfaced and fixed a real bug: Chromium's `getComputedStyle` serializes the `oklch()` tokens back out as `lab(...)`, which sigma's bundled color parser (hex / `rgb()`/`rgba()` only) silently read as opaque black — `resolveToken` now round-trips the resolved color through an actual canvas pixel (`fillStyle` + `getImageData`), which always yields sRGB bytes regardless of source color space. **Nodes**: radius bounded `[5, 18]`, scaled by `sqrt(degree / maxDegree)` in the snapshot. **Edges**: width bounded `[1, 4.5]`, scaled by the edge's own persisted `confidence` (no new API field — the closest already-available proxy for evidentiary weight); `classification === "ai_inference"` edges render dashed via a 2D canvas overlay kept in exact sync with sigma's own camera (`sigma.on("afterRender")`, `framedGraphToViewport`, `getNodeDisplayData`/`getEdgeDisplayData`, `scaleSize`) since sigma's bundled WebGL edge programs have no native dash support — those edges render at near-zero alpha in the WebGL layer (still hit-testable; picking is independent of display color) so the overlay is their only visible representation; directionality (the arrowhead) is preserved from the WebGL layer underneath. **Selection state dims rather than hides**: a selected node's neighborhood renders at full opacity, everything else at ~12% (`DIM_ALPHA`) — the P5.5 behavior of fully hiding non-neighbors is gone. The selected node gets an accent ring + always-visible label via a custom `defaultDrawNodeHover` canvas-2D draw function (sigma's only customization point for this without a bespoke WebGL border program); a selected edge gets the same accent color at increased width. **Hover** (`enterNode`/`leaveNode`) shows a tooltip with label, kind and degree. **Legend** (`src/components/investigation/graph-legend.tsx`, new): six node-kind swatches, seven relationship-type swatches, and an explicit "Dashed = AI inference" line, all reading the same token maps as the canvas. Every existing filter (`hiddenKinds`/`hiddenTypes`), the deterministic node picker, focus-on-selection mode, node/edge selection opening the shared Inspector, and Graph → Analytics/Corroboration cross-navigation are unchanged; every `data-testid` the existing e2e specs assert (`graph-canvas`, `graph-node-detail`, `graph-edge-detail`, `graph-filter-kind-*`, `toggle-focus-mode`, `graph-node-picker`, `graph-counts`, …) is preserved — no e2e spec needed a change. Validation on this commit: `tsc --noEmit` clean, `eslint .` clean, `vitest run` 466/466 (462 pre-existing + 4 new `computeLayout` determinism/connectivity tests), `next build` ok, `playwright test` 21/21 (all pre-existing, unchanged), `git diff --check` clean. M10 remains open; the Map and Timeline surfaces and the rich Evidence surface are later M10 increments. | In Progress | Claude Code (AI) | `Pending` | [docs/progress/evidence/P5.10.4/](evidence/P5.10.4/) — 7 dark-theme screenshots (full overview, focused entity + neighborhood, selected edge + Inspector, filtered graph, hover tooltip, legend, reload-determinism proof) + a pre/post comparison against the committed P5.5 circular-layout screenshot, captured against the e2e corpus by `capture.mjs` | 466 unit tests (462 unchanged + 4 new: `tests/unit/graph-layout.test.ts`) + 21 e2e specs, all unchanged and green | No |
| P6.1 | Evaluation harness (blueprint Workstream K) — an automated evaluator that independently loads persisted pipeline output and the Operation DarkNet Delhi ground truth and scores 19 metrics across entity resolution, relationship extraction, graph integrity, spatial/temporal corroboration, contradiction detection, analytics and provenance; 2 further metrics reported `NOT IMPLEMENTABLE YET` with the reason and the work required. `src/lib/evaluation/` + `scripts/evaluate.ts` (`npm run evaluate`), reports written to `reports/evaluation/`. No threshold is invented: only provenance completeness carries one, because it is the only one the project has already fixed. Ground truth is never written to. | Completed | Claude (Cowork) | `Pending` | `reports/evaluation/evaluation-summary.md` | Yes — `tests/unit/evaluation.test.ts` (20 tests) | No |
| P6.2 | Extraction now emits a person `entity_mention` from every field that NAMES a person (`subscriberName`, `holderName`, `registeredTo`, `primaryName`). `er.mentionCoverage` 84.8% → 100% (39/46 → 46/46); person entities 10 → 17. Makes a pre-existing resolver limitation visible: M1/M2/M3/X1 each split across two entities because no record anchors their identifiers together — `er.cluster.exactMatch` 100% → 66.7%, `rel.recall` 56.1% → 51.2%, precision unchanged at 100%. Resolver deliberately left unchanged; defect recorded in `docs/evaluation/resolver-failure-analysis.md`. | Completed | Claude (Cowork) | `Pending` | `reports/evaluation/evaluation-summary.md` | Yes — 486 unit tests, expectations updated where counts changed by design | No |
| P6.3 | `organisation` entity kind added to ENTITY_KINDS, graph node kinds, `--kind-organisation` token (light + dark), legend and six UI label maps. No behaviour change; evaluation identical, as designed. Resolution support deferred to P6.4 where it is exercisable. | Completed | Claude (Cowork) | `Pending` | N/A — vocabulary addition, no visual surface until a public record exists | Yes — 486 unit tests unchanged | No |
| P6.4 | `public_record` evidence type — the single controlled route for externally fetched data. `license`, `licenseUrl`, `sourceUrl`, `retrievedAt` and `registry` are REQUIRED and the content schema is `.strict()`; an invalid record is rejected at the manifest gate and emits no facts. Resolution generalised to run its Tier-A/Tier-B phases once per named subject kind (person, organisation) — existing person entity ids byte-identical. `CorpusMetaSchema.name` relaxed from a literal to a slug, `seed` nullable, so a second corpus uses the same validated ingestion path. | Completed | Claude (Cowork) | `Pending` | `reports/generalisation/generalisation-results.json` | Yes — 19 new tests (`tests/unit/public-record.test.ts`), 505 total | No |
| P6.5 | Read-only, bounded, registry-gated adapters for GLEIF (SRC-002) and Wikidata (SRC-001), `npm run collect:public` with `--dry-run`. No URL parameter and no unbounded mode exist: a source is addressed by registry id, endpoints and SPARQL are constants, `--limit` is capped by each adapter's MAX_LIMIT, and a source whose registry status is not APPROVED/APPROVED_WITH_RESTRICTIONS is refused before a socket opens (verified against OpenSanctions and NCRB). **Pilot collection attempted and BLOCKED**: egress to `query.wikidata.org` and `api.gleif.org` returns HTTP 403 at the environment proxy. Reported, not retried. | Completed (collection blocked) | Claude (Cowork) | `Pending` | `reports/generalisation/generalisation-results.json` | Yes — 14 new tests (`tests/unit/public-adapters.test.ts`), 519 total | No |
| P6.6 | Real-world entity-resolution generalisation experiment (`npm run experiment:generalisation`) over a clearly-labelled synthetic name-morphology fixture, run because real collection was blocked. Resolver frozen. Result: Tier A 7/7 anchors and 0 false merges; **Tier B fired 0 times in 24 mentions**; `unlinkedVariantRate` 82.4%; only 2 of 8 subjects recovered whole; provenance completeness 228/228. Full analysis and ranked recommendations in `docs/evaluation/resolver-failure-analysis.md`. | Completed | Claude (Cowork) | `Pending` | `reports/generalisation/generalisation-results.json` | Yes — experiment is scripted and reproducible | No |
| P6.7 | Retrieval-channel provenance. `AdapterResult` now carries `retrievalChannel` (`direct-https` \| `agent-relay`) and per-payload `sourcePayloads` hashes, and the collector writes `rawSha256Caveat` into `manifest.json` stating in words whether the hash covers publisher wire bytes or only the stored payload. Added because GLEIF proved reachable through a sanctioned operator web tool while direct egress stays 403-blocked at the agent proxy: the content is the publisher's, the bytes are not provably so, and a manifest that did not say which would be decoration rather than provenance. `--from-dir` transforms stored payloads and forces the `agent-relay` label; the `direct-https` default path is unchanged and needs no code change to re-run for a byte-exact hash. Diagnosis in `docs/data-research/network-access-diagnosis.md`. | Completed | Claude (Cowork) | `Pending` | `data/public/raw/SRC-002/*/manifest.json` | Yes | No |
| P6.8 | GLEIF adapter carries what the pilot had to validate: `jurisdiction` and `status` (both new OPTIONAL fields on `PublicRecordContent`, free text so a second publisher's spelling is not destroyed, surfaced as `public_record_jurisdiction` / `public_record_status` attribute rows), `transliteratedOtherNames` folded into aliases alongside `otherNames`, and GLEIF Level 2 relationship records mapped to the schema's existing `relations[]` with the publisher's predicate separator-folded (`IS_FUND-MANAGED_BY` → `is_fund_managed_by`) and both ends left as LEIs, never CIPHER entity ids. Multi-payload collection deduplicates by LEI so one real entity appearing in two payloads cannot masquerade as two sources agreeing. Additive and optional throughout; all 21 synthetic evaluation metrics unchanged. | Completed | Claude (Cowork) | `Pending` | `evidence/public-pilot/gleif-in-pilot.corpus.json` | Yes | No |
| P6.9 | **First real public-register data in the pipeline.** 24 real GLEIF LEI records + 1 real Level 2 relationship record (CC0 1.0, 5 payloads, 67,273 bytes, individually hashed, immutable under `data/public/raw/`) taken through raw → normalised → `public_record` → extraction → resolution → graph → measurement in a dedicated database (`data/cipher-real-pilot.db`), never mixed with the Operation DarkNet Delhi corpus. `scripts/build-pilot-corpus.ts` transforms a collected manifest into a corpus + ground truth whose only asserted claim is GLEIF's own (one LEI = one legal entity, ISO 17442); `scripts/real-data-pilot.ts` measures it. Deliberately NOT the generalisation experiment's metric set: that scores variants against an identifier-anchored variant of the same subject, a shape a register cannot produce, so every anchor-relative figure would read 100% while testing nothing. Measured: provenance completeness **327/327**, alias attachment 7/7, false merges 0, fragmentation 0 — but all 24 records resolved `new_entity`, so the zero-merge figures are arithmetic, not accuracy. Wikidata: **0 records, access blocked on every official endpoint**, so cross-source co-reference — the resolver's central function — was not tested at all. Full reading, including what the numbers do not support, in `docs/evaluation/real-data-pilot.md`. | Completed | Claude (Cowork) | `Pending` | `reports/real-pilot/gleif-pilot-results.json` | Yes | No |
| P6.10 | Resolution accepts the `alias_of` predicate that `public_record` extraction actually emits, alongside `has_alias`. Found by the real pilot, not by the suite: `extract.ts` wrote `alias_of` and `resolve.ts:367` read only `has_alias`, so every publisher-stated alias on every public record was discarded silently — 0 of 7 real GLEIF trading and transliterated names attached. One line, in the predicate allowlist only; no change to matching logic, no fuzzy step, no new tier. Real-data `aliasAttachment` 0.0% → **100.0% (7/7)**; all 21 synthetic evaluation metrics re-measured and **byte-identical**, which is why the change was in scope. The P6.6 fixture had shown this as `aliasMatchRate 0.0% (0/1)`, where n=1 read as resolver weakness rather than a wiring defect. | Completed | Claude (Cowork) | `Pending` | `reports/real-pilot/gleif-pilot-results.json` | Yes — 519/519 vitest | No |
| P6.11 | Graph synthesis stops reporting deliberate non-edges as unsupported types, and the real-data pilot reports unmapped publisher relations as a first-class result. `has_identifier` is emitted only by `extractPublicRecord` and consumed only by resolution (Tier-A matching key); like `alias_of` it is a non-edge by design, but unlike `alias_of` it was falling through to the warning branch — so a 24-record GLEIF corpus produced 25 graph warnings, 24 of them meaningless, leaving the ONE real modelling gap (`is_fund_managed_by`) as one line in twenty-five. Warnings 25 → 1. **No relationship type was invented, nothing was mapped to `ownership`, and no edge construction changed**: the GLEIF Level 2 relation remains preserved as an extracted `relationship_mention` with full provenance (`gleif:…#relations[0]`, `extraction:field-read:public_record`) and still has no graph edge, because `RELATIONSHIP_TYPES` has no honest home for fund management. The three options and why the decision is the owner's are in `docs/evaluation/real-data-pilot.md` §3.2. Also corrects that document's earlier claim that the relation was dropped silently — it warns, and the fact persists. Synthetic corpus has no `public_record` items, so DarkNet Delhi evaluation is untouched: all 21 metrics re-measured, identical. | Completed | Claude (Cowork) | `Pending` | `reports/real-pilot/gleif-pilot-results.json` | Yes — 519/519 vitest | No |
| P6.12 | Wikidata reachability fixed at the runtime, not the adapter. `query.wikidata.org` was approved and curl returned 200, yet the collector still got `HTTP 403 Host not in allowlist`: Node's `fetch` ignores `https_proxy`/`no_proxy` entirely, so the collector was going AROUND the environment's egress proxy rather than through it. `collect:public` now passes `--use-env-proxy`. The flag makes the collector honour the approved proxy; it is not a way past one. Also: the collector persisted no raw payloads while recording `rawSha256` in `manifest.json` — a hash of bytes kept nowhere, unverifiable and impossible to re-derive records from. Raw payloads are now written to `<retrievedAt>/raw/` before anything derived, with `sourcePayloads[].storedAt`; both real collections verified by recomputing sha256 from stored bytes against the manifest. | Completed | Claude (Cowork) | `Pending` | `data/public/raw/SRC-00*/*/raw/` | Yes | No |
| P6.13 | Wikidata adapter folds SPARQL solutions by item id, and GLEIF gains a bounded LEI-list lookup. A SPARQL row is one solution, not one entity: Wikidata item Q188087 states two LEIs, which crossed with a Hindi label into four rows sharing one `recordRef`, and ingestion rejected the entire corpus — correctly, since `recordRef` is `registry:registryRecordId`. 30 rows now fold to 25 records, merging identifiers and aliases, discarding nothing and choosing between nothing, with a warning naming any item stating more than one LEI. `--leis-from` asks GLEIF for exactly the LEIs an already-collected approved source published: the first pilot's 24 arbitrary Indian LEIs overlapped Wikidata on ZERO of 395,227, so a targeted lookup is both the smaller request and the only construction that produces cross-source pairs. Still no URL parameter and no unbounded mode. | Completed | Claude (Cowork) | `Pending` | `evidence/public-pilot/gleif-wikidata-cross.corpus.json` | Yes | No |
| P6.14 | **First cross-source entity-resolution measurement on real data.** 51 real records (26 GLEIF + 25 Wikidata, both CC0 1.0, both `direct-https`, both raw-hash-verified), 26 subjects, 25 cross-source pairs, run in its own database and never mixed with the synthetic corpora. Resolver **not modified**. `crossSourceJoinRate` 100% (25/25) and `identifierMatchRate` 100% — every join was `shared_identifier_merge`, 51 of 51 decisions. `exactNameMatchRate` **0%**: Tier B fired zero times, because **not one of 25 pairs had byte-identical names**, eleven of them differing only in capitalisation. The P6.6 fixture's central claim is now confirmed against real records. The four name-variation hypotheses (suffix, transliteration, abbreviation, name order) did NOT fail — but they were never tested: every such pair joined on its LEI, and Tier A never reads a name. `falseMergeRate` 4% (1/25), and it is an IDENTIFIER failure, not a name failure: Wikidata Q188087 asserts two LEIs, so Tier A transitively merged a Russian PJSC with another company. 4 of 26 Wikidata-supplied LEIs (15.4%) resolve to non-Indian entities from a query filtered `P17 = India`. Cross-source linkage inherits the linking source's error rate, and no fuzzy/embedding/LLM technique would have prevented the one failure observed. Full reading in `docs/evaluation/cross-source-experiment.md`. | Completed | Claude (Cowork) | `Pending` | `reports/cross-source/gleif-wikidata-results.json` | Yes — 519/519 vitest | No |
| P6.15 | Identifier-authority analysis and policy PROPOSAL for the cross-source false merge, plus characterization tests. **No resolver or matching logic was changed**; `resolveEntities` is byte-identical and all 21 DarkNet Delhi metrics are unchanged. Root cause located in code: Tier A unions a mention with EVERY identifier its evidence item states, and union-find is transitive, so a record asserting two values of one scheme becomes a BRIDGE — Wikidata Q188087's two LEIs collapsed a Russian PJSC and an unrelated company into one entity at confidence ≥0.9 with zero warnings, indistinguishable in the output from the 24 correct merges beside it. Four characterization tests pin CURRENT behaviour (the false merge, its silence, cross-scheme collision safety, and person/organisation kind isolation) so no future fix can change it silently; they pass unmodified. `docs/evaluation/identifier-authority-policy.md` proposes that authority is a property of (source, scheme) pairs — the issuer of a scheme is authoritative, everyone else is cross-referencing — carried as a registry column rather than code, and that Tier A reuse Tier B's existing `ambiguous_*_conflict` / `status: ambiguous` / sub-merge-confidence machinery rather than inventing any. Notes that `provenance_quality` would NOT have caught this: both sources are rated HIGH, and quality is orthogonal to authority. Four options are set out with a recommendation (authority-ranked, falling back to flag) and five decisions are left explicitly to the project owner; nothing is chosen unilaterally, and no option has been measured because none is implemented. | Proposed — awaiting owner decision | Claude (Cowork) | `Pending` | `docs/evaluation/identifier-authority-policy.md` | Yes — 523/523 vitest (4 new) | No |
| P6.15.1 | Identifier-authority policy IMPLEMENTED, as approved (Option 2 with 3 as fallback). `src/lib/resolution/identifier-authority.ts` is the whole policy and is pure: `MERGEABLE_IDENTIFIER_SCHEMES = {LEI}` (an LEI denotes exactly one legal entity, ISO 17442); a Wikidata QID never merges, because it identifies a Wikidata ITEM that editors merge, split and repurpose and that can carry several LEIs — the shape that caused the failure; a record asserting two or more distinct values of one mergeable scheme is merged on NONE of them, every value withheld rather than just the extras, because keeping the first would make identity depend on payload ordering and keeping either would be a guess carrying a merge's confidence. Schemes are isolated and unqualified values never merge. New Phase 2b gives Tier A the treatment Tier B already gave an ambiguous name — standalone entity, `ambiguous_identifier_conflict`, `status: ambiguous`, confidence 0.2, `candidateEntityIds` naming what it would have merged into, `conflicts[]` stating the authority position, and a warning — reusing the existing decision row and warning path with no UI redesign. Conflicted records are withheld from Tier B as well, or the bridge would return through a lower-confidence door. **The policy governs `has_identifier` only**: phone/account/vehicle identifiers are untouched, and a person on two phones still merges, because multi-valued is only a contradiction for a scheme where one value denotes one subject. Real cross-source: **falseMergeRate 4.0% (1/25) → 0.0% (0/27)**, one `ambiguous_identifier_conflict` raised; join/identifier/alias rates fall by exactly the one record that contradicts itself, which is the correct price. `fragmentationRate 3.8%` is a ground-truth artefact (the truth keys Q188087 by the first of its two LEIs) and was deliberately NOT corrected — editing ground truth to flatter a resolver is circular. `issues_identifier_schemes` added to the source registry as the governance record, with a test that fails if code and CSV drift or if two sources claim one scheme. DarkNet Delhi re-measured against baseline: all 21 metrics identical, snapshot counts identical, rel.precision 100% / rel.recall 51.2% unchanged, provenance 100%. GLEIF-only pilot unchanged. | Completed | Claude (Cowork) | `Pending` | `reports/cross-source/gleif-wikidata-results.json` | Yes — 545/545 vitest (26 new), eslint clean, tsc clean | No |
| P6.16.1 | **The real no-identifier corpus.** 257 real records (179 GLEIF + 78 Wikidata, both CC0 1.0, both `direct-https`, three bounded requests through the existing registry-gated collector after its `--dry-run` gate; raw payloads kept and individually hashed) built into a corpus whose IDENTIFIERS ARE ABSENT FROM THE DATA rather than suppressed behind a flag. 75 positive pairs (one subject, both publishers state the same LEI - ISO 17442, their claim, not ours), 19 hard negatives SELECTED from real records under three stated rules (TATA x4, BHARAT x3, HINDUSTAN x2, NAVNEET x2 families; the hardest is a Wikidata item that itself conflates two distinct LEIs), and 2 records left `undetermined` because a Wikidata item asserting two LEIs names no single legal entity - kept and excluded from scoring rather than dropped. `name` and `aliases` are verbatim publisher strings: NO variant is manufactured. Masked: `identifiers[]`, `registryRecordId` (surrogated `NIDP-####` - GLEIF's record id IS the LEI), `recordRef`, per-record `sourceUrl`, `relations[]`. Two regimes: FULL (nothing anchored) and ANCHORED (GLEIF keeps the LEI it ISSUES, every Wikidata record stripped, so the SHARED identifier is unavailable). SEC EDGAR was considered and rejected: it publishes no LEI, and the only CIK->LEI bridge runs through Wikidata, whose identifier error rate P6.14 measured at 15.4%. Own evidence dir, own databases, own reports; never mixed with DarkNet Delhi, the GLEIF-only pilot or the cross-source identifier evaluation. | Completed | Claude (Cowork) | `eb1330c` | `evidence/no-identifier/`, `data/public/raw/SRC-00{1,2}/2026-09-03T20-51-*` | Yes - `tests/unit/no-identifier-corpus.test.ts`, 11 masking-invariant assertions | No |
| P6.16.2 | **Baseline of the UNMODIFIED resolver on that corpus. `src/lib/resolution` is byte-identical to P6.15.1.** Result: `positivePairJoinRate` **0.0% (0/75)** in BOTH regimes - not one real cross-source pair was joined without a shared identifier. Tier B fired **zero** times: in FULL it cannot (no Tier-A cluster exists to match into, so even byte-identical names would not merge), and in ANCHORED it can but does not, because **0 of 75 pairs have byte-identical names** - the fourth independent corpus to show this. Failure classes on real publisher strings: legal suffix 29 ('ICICI BANK LIMITED' / 'ICICI Bank'), capitalisation 24 ('STATE BANK OF INDIA' / 'State Bank of India' - GLEIF upper-cases legal names, Wikidata title-cases them), abbreviation 9 ('FLIPKART INDIA PRIVATE LIMITED' / 'Flipkart'), divergent 12 ('INNOFIN SOLUTIONS PRIVATE LIMITED' / 'LenDenClub'), transliteration 1. **The more serious finding is that the failure is SILENT**: 0 warnings, `unresolvedRate` 0.0%, `falseMergeRate` 0.0%, and all 257 decisions `status: resolved` / `new_entity` - the run is indistinguishable in the output from a perfect one while asserting 257 entities where ground truth has 182. `hardNegativeFalseMergeRate` 0.0% (0/19) is recorded but is analytically EMPTY: nothing merged, so nothing merged wrongly; it becomes evidence only once something can merge on a name. Aliases (107 publisher-stated, incl. 18 Devanagari) are stored and provenanced but read by NO tier as match candidates. Per-record leak check CLEAN in both regimes. No matching change proposed - P6.16.3 decisions are listed in the doc's section 8. | Completed | Claude (Cowork) | `eb1330c` | `reports/no-identifier/{full,anchored}-results.json`, `docs/evaluation/no-identifier-experiment.md` | Yes - 556/556 vitest (11 new), eslint clean, tsc clean; DarkNet Delhi all 21 metrics identical and cross-source identical after the work | No |
| P6.17.1 | **Deterministic name normalisation, and Tier B2.** `src/lib/resolution/name-normalization.ts` is the whole rule set and is pure and idempotent: Unicode NFKC, case folding, punctuation folding (apostrophes DELETED not spaced, so `Dr. Reddy's` gives `dr reddys`; `&` expands to ` and `), whitespace collapsing, then trailing legal-suffix stripping longest-first and never to empty. NO fuzzy matching, no edit distance, no threshold, no embeddings, no ML - two names either normalise to the same key or they do not. Wired in as Tier B2 behind the exact-match branch, as its own `normalized_name_match` type at its own confidence (0.55, strictly below exact-name 0.6 and far below shared-identifier 0.95) so name evidence can never be mistaken for identifier evidence in a decision row; a normalised key reaching 2+ clusters is `ambiguous_normalized_name_conflict` at 0.2, below the merge floor. Deliberately NO transliteration, NO token reordering and NO subset matching - `GVK` is a subset of every `GVK ...` entity, which is the rule the hard negatives exist to catch. Real-data effect (anchored regime, 75 real pairs): `positivePairJoinRate` **0.0% -> 70.7% (53/75)**, capitalisation **24/24** and legal suffix **29/29** both closed whole, abbreviation/divergent/transliteration untouched by design, `hardNegativeFalseMergeRate` **0.0% (0/19)**, Tier B firings 0 -> 54. FULL regime stays 0/75 and that is structural, not a rule failure: Tier B can only match INTO a Tier-A cluster and FULL has none. | Completed | Claude (Cowork) | `169e15d` | `reports/no-identifier/anchored-results.json`, `docs/evaluation/name-normalization-and-resolution-semantics.md` | Yes - `tests/unit/name-normalization.test.ts` (31) + `tests/unit/resolution-normalization.test.ts` (24), incl. all 19 real hard negatives asserted apart | No |
| P6.17.2 | **Silent non-resolution fixed.** `RESOLUTION_STATUSES` gains `unresolved`, and the two meanings sharing `new_entity` are split: `new_entity`/`resolved` now means ESTABLISHED FROM ITS OWN IDENTIFIER, while an uncorroborated mention becomes `unlinked_mention`/`unresolved`. It still becomes its own entity - nothing is ever dropped - but it is no longer reported as a success. Each such decision's `reason` names BOTH keys that were searched (the exact name and the normalised name), so a reader can see why a real positive pair failed; one AGGREGATE warning per subject kind, not one per mention (P6.11 removed 24 meaningless warnings for that reason). Surfaced with no UI redesign: `ResolutionSummary` gains `decisionsByStatus`/`unresolvedDecisions`, `ResolvedEntityView` gains `isUnresolved`, and the summary panel reuses the existing ambiguous-note component verbatim. **The defect was live on DarkNet Delhi too** - 9 of 88 person mentions had never resolved and the run reported nothing; a unit test asserted `warnings).toEqual([])` and passed, pinning the defect as correct. That test was inverted. All 21 DarkNet Delhi metrics, the snapshot (61 entities / 191 relationships) and the GLEIF x Wikidata cross-source run are **identical**: the clustering did not change, only what the system says about it. | Completed | Claude (Cowork) | `58860e2` | `reports/evaluation/evaluation-results.json` | Yes - regression tests that unresolved/ambiguous cannot look like success, plus the inverted DarkNet Delhi assertion | No |
| P6.17.3 | **Devanagari primary-name pilot.** Source: Wikidata Hindi labels (SRC-001, CC0 1.0, APPROVED). Wikidata gives each item ONE label per language plus separate aliases (`skos:altLabel`), so the Hindi `rdfs:label` IS the publisher's primary Hindi name (Help:Label); our adapter folded it into this schema's single `aliases[]` because `PublicRecordContent` has one `name`, which is OUR modelling artefact, not the publisher's classification. **Nothing was collected** - the Hindi labels were already in the P6.16 raw SPARQL payload (`SRC-001/2026-09-03T20-51-29-042Z`, rawSha256 `657c11d624a36105...`, direct-https). The English label is DROPPED, not demoted, so no Latin form of the name reaches the resolver. Nothing manufactured. Bounded pilot: 55 of 78 items carry a Hindi label, all 55 with a GLEIF counterpart; 234 records, 54 positive pairs, 19 hard negatives. Result: **51 real Devanagari-Latin primary-name pairs, 0 joined, 100% failure** - the transliteration hypothesis is finally tested at N=51 instead of P6.16's single incidental Japanese pair. Expected, not a regression: normalisation deliberately does not fold script. `hardNegativeFalseMergeRate` 0.0%, `falseMergeRate` 0.0%, provenance 100%. | Completed | Claude (Cowork) | `876f45a` | `reports/no-identifier/devanagari-pilot-anchored-results.json` | Yes - covered by the shared experiment harness and its per-record leak check (CLEAN) | No |
| P6.17.4 | **Publisher-stated aliases evaluated, NOT enabled.** `scripts/alias-evidence-study.ts` imports no resolver, runs no pipeline and writes no database: it replays the resolver's own normalisation over the same real corpus and reports what an alias-aware Tier B WOULD have decided. Measured over 107 publisher-stated alias strings: recall 70.7% (53/75) -> **72.0% (54/75)**, i.e. **+1 pair**; 0 pairs newly broken; **0** joined to the wrong subject; **0** became ambiguous; **0 of 19** hard-negative pairs share a normalised key; **0** keys reach more than one subject. The single gain is POS-022 `Tata Motors Ltd` -> `TATA MOTORS PASSENGER VEHICLES LIMITED` via GLEIF's own alias - which is precisely the parent/subsidiary trap P6.16 flagged, so the sole measured benefit is the join whose correctness is most debatable. If ever enabled, a decision must name WHICH alias matched and WHICH publisher stated it; an alias-justified merge is only auditable if the alias is attributable. **Recommendation: do not enable** - one join for a whole new matching surface, on a corpus containing no shared trading name, which is the risk that would actually bite. | Completed | Claude (Cowork) | `876f45a` | `reports/no-identifier/alias-evidence-study.json` | Yes - measurement script, no behaviour to test | No |
| P6.18.1 | **The 22 remaining real positive failures analysed individually, and two of the three failure buckets shown to be mixtures.** `scripts/deterministic-evidence-study.ts` follows the P6.17.4 pattern - imports no resolver, runs no pipeline, writes no database - and replays the shipped `normalizeName` over the same real corpus. Every candidate rule is scored over ALL 257 records (32,896 candidate pairs), not merely the 22 it targets, because scoring only the targets measures benefit and never cost; merges are unioned, since the resolver clusters by union-find and transitivity is where one bad edge does its damage. The harness validates itself before any delta is read: the baseline rule must reproduce the independently measured 53/75 or declare itself INVALID. It reproduces it exactly. Reclassification: the corpus's 9 "abbreviations" are one SHIPPED-RULE DEFECT (`bv` IS in `LEGAL_SUFFIXES` but punctuation folding splits `B.V.` into `b v` first, so it could never match), one leading article, two publisher-name-availability cases and five genuine containment cases; the 12 "divergent" names are three cases where the publisher states the legal name and it matches, one where it states it and does NOT, three group/operating-company relationships, four true brand names with no published bridge, and one acronym-of-its-own-expansion. | Completed | Claude (Cowork) | `Pending` | `reports/no-identifier/deterministic-evidence-study.json` | Yes - measurement script, self-validating against the P6.17.1 baseline | No |
| P6.18.2 | **Bounded abbreviation experiment. Ordered containment is materially safer than unordered, and the 19 real hard negatives are what shows it.** Measured, NOT enabled. R1 (trailing dotted legal form) +1, R2 (leading definite article) +1, R5 (publisher-stated official name) +5, guarded prefix containment +8; combined **53/75 (70.7%) -> 66/75 (88.0%)** with **0/19 hard-negative merges, 0 corpus-wide false merges, 0 false edges of 70 proposed and 0 pairs broken**. **Unordered token subset breaks NEG-013** (`BHATI SOLAR SOLUTIONS` is a subset of `RAJDEEP BHATI SOLAR SOLUTIONS`, two distinct LEIs) at BOTH length thresholds; ordered prefix containment does not, because the hard negatives diverge at their second token. A minimum-token floor does not fix unordered subset - ordering does. **Zero measured cost is recorded as NOT zero risk**: prefix containment produced only 8 relations in 257 records and all 8 are true positives, but **12 of 175 leading tokens (6.9%) already span more than one subject** (`tata` 4, `bharat` 3), and the four single-token cases simply have no sibling here - `GVK` is a prefix of every `GVK ...` entity that exists. The rule is therefore admissible only WITH the uniqueness guard reusing Tier B2's existing `ambiguous_normalized_name_conflict`; the guard suppressed 0 relations, so it is **correct by construction but entirely unexercised by this corpus**, and that is stated rather than glossed. POS-022 (`Tata Motors Ltd` / `TATA MOTORS PASSENGER VEHICLES LIMITED`) is flagged as a DISPUTED win - the P6.17.4 parent/subsidiary trap, right for the wrong reason. | Completed | Claude (Cowork) | `Pending` | `reports/no-identifier/deterministic-evidence-study.json` | Yes - all 19 real hard negatives scored under every candidate rule | No |
| P6.18.3 | **Bounded transliteration experiment at N=51, and the sharpest result of the phase.** `scripts/transliteration-study.ts` implements a deterministic Devanagari->Latin table (independent vowels, matras, consonants, nukta forms, virama, anusvara, word-final schwa deletion) plus a decoder for English letter-names written in Devanagari (`एसबीआई` -> `sbi`). Nothing manufactured: every Devanagari string is Wikidata's own `rdfs:label@hi`, every Latin string GLEIF's own legal name. Result **0/51 -> 1/51 -> 2/51 (3.9%)**, `hardNegativeFalseMergeRate` 0.0% and 0 false merges at every stage. **The transliterator works and still joins almost nothing: transliteration converts a SCRIPT problem into a SPELLING problem, and exact matching cannot close a spelling gap by construction.** The residual splits cleanly and the split is the finding - **13 of 49 unjoined pairs are within edit distance 3** (`ashok leland`/`ashok leyland` d=1, `bank of barauda`/`bank of baroda` d=2, `aiksis bank`/`axis bank` d=3) and are the same name spelled differently, while **36 of 49 are beyond it** and are not transliterations at all but TRANSLATIONS (`भारतीय इस्पात प्राधिकरण` = "Steel Authority of India"), which no script folding reaches. Edit distance is reported ONLY to characterise the residual; nothing merges on it and no fuzzy rule is proposed. Recorded negative result: Hindi medial schwa deletion was implemented, measured and **gained nothing**, and a first version made the output strictly worse - the returns going flat is itself evidence the residual is lexical rather than rule-shaped. | Completed | Claude (Cowork) | `Pending` | `reports/no-identifier/transliteration-study.json` | Yes - measurement script, scored against the same 19 hard negatives | No |
| P6.18.4 | **Evidence-availability probe: the divergent class needed a missing FIELD, not a missing algorithm.** The corpus audit found that corroborating attributes are structurally unavailable across the pair - `jurisdiction`, `status` and `observedAt` are published by GLEIF for all 179 records and by Wikidata for **none** - and, more importantly, that **corroboration does not generate candidates, it only validates them**: no attribute joins `ORAVEL STAYS LIMITED` to `OYO Rooms`. What the class needs is a LINKING CLAIM. Wikidata publishes one - `P1448` official name - and the adapter never asked for it. A bounded probe over **exactly the 78 items already in the corpus** (QIDs recovered from the STORED raw SPARQL payload `SRC-001/2026-09-03T20-51-29-042Z`, so no item was discovered and collection was NOT broadened) found `P1448` on 8 items (10.3%) and `P1813` on 3 (3.8%). Under the SHIPPED normaliser it closes **5 of the 22** - POS-004, POS-006, POS-009, POS-029, POS-074 - spanning both the divergent and abbreviation classes, using a publisher's own claim and **no new matching rule at all**. POS-020 publishes `P1448` and still does not bridge (`emlyon business school` is not `EARLY MAKERS GROUP`): availability is not sufficiency. Provenance recorded honestly - the probe is **`agent-relay` and explicitly NOT admissible as collected evidence**, because this session's local runtime has no egress; promoting it requires re-collection through `collect:public` on the `direct-https` path, a query change needing no code change. | Completed | Claude (Cowork) | `Pending` | `evidence/no-identifier/wikidata-official-name-probe.json` | Yes - probe result re-verified against GLEIF names through the shipped normaliser | No |
| P6.18.5 | **ML gate: NO-GO, with the one place a GO could later become defensible named precisely.** No resolver change was made in this phase: `src/lib/resolution/` is byte-identical to P6.17 (directory diff), so **612/612 vitest, all 21 DarkNet Delhi metric values, the GLEIF x Wikidata cross-source run and both no-identifier regimes are unchanged**; `tsc` and `eslint` clean. Ground truth untouched in both corpora; POS-023 (`Paytm` / `ULTRAVIOLET TECH PRIVATE LIMITED`) is recorded as a SUSPECTED publisher-data issue resembling P6.14's 15.4% Wikidata identifier error rate and was deliberately **not** corrected, on the P6.15.1 principle that editing truth to flatter a resolver is circular. NO-GO rests on four measurements: the largest recoverable group was closed by a publisher's own claim rather than inference; deterministic rules reach 88.0% at zero false merges, one of the wins being a defect fix mislabelled as a semantic failure for two phases; **8 of the 9 remaining failures are not name problems** (`ORAVEL STAYS LIMITED` and `OYO Rooms` share not one character, and three more are group/subsidiary pairs where a name join would be WRONG even when it scores right); and there are **no labels** for the one class where a model would plausibly help - the transliteration residual is 13 real pairs, an evaluation set rather than a training set. A GO would be defensible for transliteration spelling-variant matching ALONE, and only after a licence-compatible pair list is collected (never generated), a held-out set that is not these 51 pairs exists, and a **product** decision is taken about what a decision row says when the evidence is "the model was confident". Five proposals are left to the owner with a recommendation (approve official-name re-collection, R1 and R2 - all zero-cost, R1 being a defect fix; treat guarded containment as a separate decision needing a corpus that contains the trap; settle parent/subsidiary policy first). Two environment findings recorded: the evaluation report is **not byte-reproducible** (two runs on identical code differ in `corroboration.*.recall` `findingId`/`findingType` while all 21 metric VALUES stay stable, so "the report is identical" is too strong a regression check), and the repository **cannot run its own suite in place** on the synced folder (SQLite `disk I/O error`, 52 of 612 tests fail environmentally; all 612 pass on a scratch copy outside the mount). **No ML training has begun.** | Proposed - awaiting owner decision | Claude (Cowork) | `Pending` | `docs/evaluation/deterministic-evidence-and-ml-gate.md` | Yes - 612/612 vitest, full regression re-measured and identical | No |
| P6.19.1 | **Audit of the real corpus as it stood, and three structural defects a record count would never have shown.** `scripts/corpus-audit.ts` is read-only and recollects nothing. 257 records / 180 distinct LEIs / 75 positives / 19 hard negatives / 2 undetermined / 0 duplicates. The findings: **exactly ONE source pairing** - all 75 positives `gleif x wikidata`, all 19 hard negatives `gleif x gleif`, so anything measured there learns GLEIF house style versus Wikidata house style rather than entity resolution; **99.2% Latin script** (255/257, one Cyrillic, one Han+Kana), because the linkage set was filtered to India; **zero relationship evidence**, `relations[]` being masked by the corpus builder; and **the second corpus was never independent** - the Devanagari pilot shares 54 of its 75 subjects with the main corpus, so it could not serve as a held-out set. Also recorded: 10 subjects appear in both a positive pair and a hard negative, which is the number that matters if these pairs are ever split into train and evaluation. | Completed | Claude (Cowork) | `Pending` | `reports/corpus-audit/corpus-audit.json` | Yes - read-only audit script | No |
| P6.19.2 | **A third publisher, a worldwide linkage set, and the fields the adapters were discarding.** SEC EDGAR was already SRC-006 APPROVED_WITH_RESTRICTIONS, so no new approval was needed; its 10 req/s limit and mandatory contact User-Agent are enforced in the adapter, not by convention, and only the company-level block is read so no natural person is collected. **P6.16's rejection of EDGAR was re-tested and CONFIRMED against the tempting reading**: the submissions schema has an `lei` key but it was null for all 20 filers probed, so EDGAR does not join GLEIF, it joins Wikidata through the CIK. Wikidata gains `companies-with-lei-enriched` - no country filter, plus `P1448` official name, `P1813` short name, `P1320` OpenCorporates id and `P5531` CIK; GLEIF now carries the `ocid` and the national register number (`registeredAs` qualified by `registeredAt`) it was already publishing and the adapter was dropping. `officialName` is a new OPTIONAL schema field, deliberately NOT an alias - an alias is any other name, an official name is a legal-name claim, and P6.17.4 recommended aliases not be enabled as merge evidence, so conflating them would silently settle an open question. It is carried and provenanced and read by NO tier. Collected `direct-https`, raw payloads written before anything derived and individually hashed: **663 Wikidata** (`b2e563fe...`), **500 GLEIF** (`ecbae433...`), **100 EDGAR** (`f09874c3...`). Official-name coverage **10.3% -> 33.3%**; 342 OpenCorporates ids; 98 CIKs; 53 of 100 filers carry SEC-stated former names. `issues_identifier_schemes=CIK` added to SRC-006 and `CIK: "edgar"` to `SCHEME_ISSUER_REGISTRY` so code and registry cannot drift - declaring an issuer does NOT make a scheme mergeable, and `MERGEABLE_IDENTIFIER_SCHEMES` is still `{LEI}`. | Completed | Claude (Cowork) | `Pending` | `data/public/raw/SRC-00{1,2,6}/2026-09-04T03-0*` | Yes - 615/615 vitest, tsc and eslint clean | No |
| P6.19.3 | **The expanded ground truth - and a labelling rule of my own that had to be corrected.** 257 -> **1,245 scorable records**, 75 -> **578 cross-source positives**, 19 -> **146 hard negatives**, plus **79 former-name pairs kept as their OWN class** (the SEC's temporal claim about one CIK is not two publishers agreeing and must never be counted as though it were). Positive source pairings 1 -> 2, negative pairings 1 -> 5, script-variant positives ~1 -> 31, divergent 12 -> 69, and 193 positives independently corroborated by an OpenCorporates id neither publisher issues. **A label is created only by a shared authoritative identifier or an explicit publisher assertion, never by a name; no variant is manufactured and no model-generated label is used.** THE CORRECTION: the first build produced 320 hard negatives of which the resolver appeared to falsely merge 117 - inspection showed they were not negatives at all (`UBER TECHNOLOGIES, INC.` from GLEIF and `Uber Technologies, Inc` from EDGAR are one company). The rule had tested *different identifier strings* rather than *disagreement within a shared scheme*, and since EDGAR publishes no LEI and GLEIF no CIK, every correctly-resolved GLEIF x EDGAR pair was scored as a false merge. A negative now requires comparability; **94 name collisions are recorded NOT COMPARABLE and scored as neither**, and the same flaw was fixed in the corpus-wide false-merge metric where it had inflated the rate from 0.1% to 6.5%. Leakage: every subject used by the P6.16 evaluation corpus is EXCLUDED so that instrument stays held out, and the remainder is split **by subject, never by pair** (278 training-candidate / 302 held-out), verified unbiased at 40.9% vs 40.5%. | Completed | Claude (Cowork) | `Pending` | `evidence/expanded/expanded.ground-truth.json` | Yes - leak check CLEAN, split-bias check reported | No |
| P6.19.4 | **The shipped resolver re-measured on a harder corpus, and the defect that corpus found.** Full pipeline in its own database, never mixed with DarkNet Delhi, the synthetic fixtures or the P6.16 corpus; leak check CLEAN; provenance **100% (13,772/13,772)**. `positivePairJoinRate` **40.7% (235/578)**, `falseMergeRate` **0.1% (1/994)**, `hardNegativeFalseMergeRate` **0.7% (1/146)**. **The fall from 70.7% is not a regression** - the resolver is unchanged and the old corpus was India-filtered and dominated by the case and suffix classes normalisation already solves. By class: identical 97.7%, case-only 98.0%, legal-suffix 92.3%, and containment **0/160**, partial overlap **0/69**, divergent **0/69**, script variant **0/31**. THE DEFECT: the first run died with `UNIQUE constraint failed: aliases.id`, taking the WHOLE resolution stage down so every metric read 0. `makeContentId` lower-cases its parts, so an alias id is case-insensitive by design and GLEIF's real `otherNames` pair `PIONEER RAILCORP` / `Pioneer Railcorp` is one row - but the emitter keyed its dedupe map on the raw string and persistence then inserted one id twice. The 257-record corpus simply never contained such a pair. Fixed by making the emitter agree with the id scheme (canonical key, publisher's casing kept, existing lowest-source-id tie-break); **no tier, threshold, rule or confidence changed**, and DarkNet Delhi (all 21 metric values), the cross-source run and both no-identifier regimes re-measure identically. | Completed | Claude (Cowork) | `Pending` | `reports/expanded/expanded-anchored-results.json` | Yes - `tests/unit/resolution-alias-id.test.ts` (3 tests, real publisher strings), 615/615 vitest | No |
| P6.19.5 | **ML readiness: NOT JUSTIFIED - deterministic/evidence methods preferred.** `scripts/expanded-deterministic-ceiling.ts` (measurement only, nothing enabled) prices the rules already on the table against **146 real hard negatives instead of 19**: shipped 249/578 with 3 merged; + official name **344 (59.5%) with 4** - **+95 pairs at NO additional precision cost**, the largest available gain in the project and from a field the publisher hands over for free; + dotted-form and article 357 (61.8%) with 5; + guarded prefix containment **450 (77.9%) but hard-negative merges 5 -> 10 (3.4% -> 6.8%)**. **P6.18's written warning that "zero measured cost is not zero risk" for containment is now confirmed** - it measured 0/19 on a corpus containing no counter-example, this one contains them, and the uniqueness guard fired 9 times against 0 before. The ceiling study is stated as an UPPER BOUND (it unions any two records sharing a key where Tier B can only match into an existing Tier-A cluster: 249 vs the pipeline's 235, ~2.4pp optimistic). The verdict rests on the residual not yet being sizeable: +95 pairs come from a published field, the next-largest class is deterministic but now demonstrably costs precision, divergent names (93) are not a string problem at all (`ORAVEL STAYS LIMITED` / `OYO Rooms` share no character), and 31 script variants is too thin to train on. Hard negatives at 146 against 578 positives also remain inverted for a task that is negative-dominated in reality. Five decisions are left to the owner with a recommendation; **no ML training, embeddings, LLM matcher or fuzzy matching was added.** | Proposed - awaiting owner decision | Claude (Cowork) | `Pending` | `docs/evaluation/expanded-corpus-and-ml-readiness.md` | Yes - full regression re-measured and identical | No |
| M8 | Investigation Copilot (Workstream G) | COMPLETE — G1 (grounded retrieval), G2 (grounded answer generation with per-claim citation, confidence and classification, plus explicit insufficient-evidence handling) and G4 (an automated output-boundary anti-hallucination guardrail — classification enforcement before any model call, fabricated-literal and unsupported-assertion rejection on model output, and citation resolution against the live store) are all implemented and covered by 105 unit tests and 4 real-app E2E tests with visual evidence (P5.8). A language model never contributes a fact: every claim is built deterministically from a persisted record and carries its classification and confidence; a model is used for wording only and its output is discarded whole on any guardrail failure, with the deterministic narration served instead. All eight canonical demo-contract questions answer, fully cited and classified. **G3 (multi-turn follow-up / reasoning modes beyond direct Q&A) is a documented scope-cut per `docs/implementation-blueprint.md` §6** and is not implemented. Reconciling the deterministic Copilot's grounded output against the ground-truth narrative — including the q2/q4/q6/q7 divergences and the non-deterministic q7 binding recorded in `docs/data/copilot.md` §8 — is Workstream K / M12's concern. | Claude Code (AI) | `Pending` | [docs/progress/evidence/P5.8/](evidence/P5.8/) | 105 unit + 4 E2E | Yes |
| M9 | Dossier / Report (Workstream H) | COMPLETE — H1 (report content model), H2 (generation with traceability enforcement) and H3 (rendered review surface) are all implemented and covered by unit and real-app E2E tests with visual evidence (P5.9). Every claim in a generated report carries an evidence classification and resolves to persisted ids, enforced before write rather than checked after; generation is deterministic, idempotent, and never requires a live AI request. No export surface was built — no existing contract requires one, and H3 is satisfied by a rendered, reviewable in-app report. | Claude Code (AI) | `d593074` | [docs/progress/evidence/P5.9/](evidence/P5.9/) | 58 unit + 3 E2E | Yes |
| M10 | UI / Visual Investigation Experience (Workstream I) | In Progress — P5.10.2 (command-centre shell), P5.10.3 (shared Inspector + persistent focus) and P5.10.4 (graph surface redesign) landed; the Map and Timeline surfaces and the rich Evidence surface remain | Claude Code (AI) | See P5.10.2–P5.10.4 | Required at completion | Required at completion | No |
| M11 | Integration Hardening (Workstream J) | PLANNED | Unassigned | — | Required at completion | Required at completion | No |
| M12 | Evaluation (Workstream K) | PLANNED | Unassigned | — | Required at completion | Required at completion | No |
| M13 | Stabilization, Demo Rehearsal & Presentation (Workstream L) | PLANNED | Unassigned | — | Required at completion | Required at completion | No |

Rows for P0.14–P0.17 (secret validation, repository validation, initial commit, GitHub synchronization) are process steps rather than tracked features and are not entered as ledger rows; their outcomes are reported in the task completion summary.

**M1–M13 above are milestone-level PLANNED placeholders**, not yet-completed claims. Each milestone decomposes into individually AI-delegable tasks (IDs A1–L4) fully specified in `docs/implementation-blueprint.md` §4 — those 40+ task-level rows are intentionally not pre-populated here, since a row with no owner, commit, or evidence yet would immediately go stale; a task row is added to this ledger at the point its owning agent actually begins work on it, per this ledger's own append-only, status-must-reflect-reality convention (`docs/progress/README.md`).

Application features are recorded as individual task rows above as they are implemented: ingestion (P5.2), extraction (P5.3), entity resolution (P5.4), graph synthesis (P5.5), topology analytics (P5.6), spatial/temporal corroboration (P5.7), the Investigation Copilot (P5.8), and the dossier/report (P5.9).

**The Investigation Copilot (Workstream G) gap is closed.** Its core implementation landed early (commits `badd9de` and `87bcd1f`) and was consumed by P5.9 before it had a ledger row, `docs/data/copilot.md`, tests, or visual evidence — so it was deliberately not claimed complete. P5.8 (this row) now supplies all of them: `docs/data/copilot.md`, `tests/unit/copilot.test.ts` (105 tests, including G1 retrieval-recall and G2 correctness against the held-out ground truth), `tests/e2e/investigation-zzz-copilot.spec.ts` (4 real-app tests), and `docs/progress/evidence/P5.8/`. Per the Completion Definition in `docs/requirements.md` §13, M8 is therefore complete (G3 excepted as a documented §6 scope-cut).
| P6.20.1 | **The rule table re-priced, and two "zero cost" claims withdrawn.** `scripts/rule-attribution.ts` is measurement only - imports no resolver, runs no pipeline, writes no database - and rebuilds each proposed rule's merge graph with LABELLED edges, reconstructing the path between every merged pair so each merge names the exact key that caused it. P6.19's handoff recommended the official-name rule at "+95 pairs, zero cost" and R1/R2 at "+13, zero cost"; `reports/expanded/deterministic-ceiling.json`, written by that same phase, records hard-negative false merges going 3 -> **4** and 3 -> **5**. The prose read the recall column and not the precision column. Named: **EN-0124** `GENERTEL S.P.A.` [815600DEEE5337E9A213] == `Genertel` [549300EWN48Q47PAWX22] (official name), and **EN-0103** `SIMON PROPERTY GROUP, INC.` [529900GQL5X8H7AO3T64] == `SIMON PROPERTY GROUP, L.P.` [MR92GTD0MJTTTTQDVG45] (R1). EN-0103 is a rule defect and not bad luck: `stripDotted` consumes the trailing `l p`, the shipped normaliser already strips `inc`, so the ONLY token separating a REIT from its operating partnership is the one R1 removes - the same failure as the pre-existing `ENDESA`/`ENDESA SA` merge, widened rather than created. Nothing enabled. | COMPLETE | AI | `a00cdf3` | N/A | Measurement script; no resolver change | N/A |
| P6.20.2 | **Level 2 collected - the approved half of SRC-002 that nothing had ever requested.** SRC-002 is registered "GLEIF LEI (Level 1 + Level 2)", APPROVED, CC0 1.0, so no new approval was needed. The adapter has had `mapGleifRelationship`, `normaliseGleifPredicate` and a `relations` parameter on `mapGleifRecord` since an earlier phase, and **none of it could ever fire**: `collectGleif` called only `/lei-records`, and no `/lei-records` payload contains a relationship record. Relationship coverage read 0 because nothing asked, not because GLEIF withholds it. `collectGleif` now takes `withRelationships` and fetches `direct-parent-relationship` and `ultimate-parent-relationship` per LEI. BOTH are required: BNP PARIBAS CARDIF POJISTOVNA's direct parent is BNP PARIBAS CARDIF, an intermediate holdco sharing no distinguishing token with the group, and only the ultimate parent reaches BNP PARIBAS. No children endpoints - those are paged collections whose size is a property of the parent, not of the request, so they are not bounded by construction. **HTTP 404 is the publisher's answer, not a failure** (the entity states no parent of that kind) and is counted separately rather than aborting the run. Bound: **345 of 661 LEIs**, selected by `scripts/build-relationship-linkage-set.ts` on a PAIR-CLASS rule fixed before any fetch - every LEI in a hard negative (111), containment (151), partial-overlap (68) or **divergent** (64) pair; divergent is included precisely because a parent edge is not expected to help there, and omitting it would leave a sample that could only flatter the result. **345 records, 5 manifests, all `direct-https`, raw payloads written before anything derived and individually hashed; 82 LEIs state a parent, 154 edges** (76 direct, 78 ultimate). | COMPLETE | AI | `a00cdf3` | N/A | `tests/unit/public-adapters.test.ts` (+6: inert without an LEI list, request count priced into the dry-run plan, MAX_LIMIT respected, predicate folding, non-LEI ends refused, relation attached without asserting identity) | N/A |
| P6.20.3 | **Publisher-stated ownership priced as a false-merge guard, and guarded containment still fails.** `scripts/relationship-evidence-study.ts`, measurement only. Guard: two records with DIFFERENT LEIs are publisher-related when GLEIF states a consolidation edge either way, or both consolidate to the same ultimate parent - evidence they are TWO entities, never evidence to merge. **33/146 hard negatives (22.6%) carry such an edge; 0/578 true positives would be blocked; 0/500 seed-fixed random distinct-LEI control pairs are related (0.0%).** The 22.6%-vs-0.0% contrast is the result: the signal is specific to the pairs the corpus says must not merge, not a background property of any two companies. The middle row is the falsification test and it is measured over all 578 positives, not argued from the definition. **But the guard does not rescue guarded prefix containment: 10 -> 7 false merges** (stops EN-0129, EN-0130, EN-0143), still more than double the shipped 3, and both headline group pairs survive - GLEIF returns 404 for both parent relations on both Telstra LEIs (TELSTRA GROUP carries a `NO_KNOWN_PERSON` reporting exception, and the edge upward from TELSTRA CORPORATION is simply unpublished), and neither Cultura LEI states a parent. A real publisher coverage limit, reported as one. Consequence for the roadmap: the containment residual is at least partly a RELATIONSHIP problem whose correct output is an edge the schema already represents, not a merge - and decision 4 (parent/subsidiary policy) must precede decision 3, because until "parent" has a defined output "false merge" has no stable definition to measure against. Nothing enabled; `src/lib/resolution/` byte-identical to `cf466a0`. | COMPLETE | AI | `a00cdf3` | N/A | Measurement script; DarkNet Delhi all 21 metric values re-measure identically | N/A |
| P6.21.1 | **The product is CIPHER.** Project-wide rename from "NetIntel AI" to **CIPHER** across all 80 tracked files that carried the old name - 145 replacements covering prose, every doc, the LICENSE and .gitignore headers, UI strings and page title, the npm package name, all SQLite database paths, the localStorage and `Symbol.for` keys, the crawler `User-Agent` product token, and the Copilot's allowed-vocabulary entry. **Two classes of occurrence are deliberately preserved and neither is an oversight.** (1) The GitHub repository slug `devv0311/netintel-ai` - in the README repository tree, `docs/repository-governance.md`, this ledger's P0.5 row, the P0 repository audit, and inside the GLEIF and Wikidata `User-Agent` CONTACT URLs. The repository is not renamed: the slug is the published identity of every commit already pushed and the User-Agent URL is a contact address that has to resolve. Product name and repository identity are now recorded as separate things in `docs/repository-governance.md`. (2) The seed literal `netintel-p6.20-control-${i}` in `scripts/relationship-evidence-study.ts` - not a name but the hash input that fixes WHICH 500 pairs form P6.20's control set; renaming it would draw a different sample and silently change the published 0/500 result. Frozen, with the reason stated at the call site. No behaviour changed; `src/lib/resolution/` and `src/lib/graph/` byte-identical to `a00cdf3`. | COMPLETE | AI | `be02319` | N/A | 621/621 vitest re-run after the rename, identical to the pre-change baseline; `tsc --noEmit` and `eslint .` clean | N/A |
| P6.21.2 | **Parent/subsidiary decision memo - P6.20 decision 4, answered but NOT implemented.** `docs/evaluation/parent-subsidiary-policy.md`. Establishes that a publisher-stated relationship is dropped at the GRAPH boundary, not the collection boundary: all 154 edges are collected, provenanced and extracted as `registry_relation` mentions, then skipped with a warning because `RELATIONSHIP_TYPES` has no honest home for them - and `ownership` is unavailable because it already means person -> identifier (38 of DarkNet Delhi's 196 edges). New measurements from artifacts already on disk, no request issued: only **30 of 154 edges (19.5%) have both ends held as records - 18 distinct pairs - while 124 (80.5%) name one of 72 LEIs the corpus does not contain**; of the 30, 20 share a name token and 10 share none, and 16 cross a jurisdiction boundary, so name similarity is not a proxy for the relation in either direction; the 33 publisher-related hard negatives decompose into **20 directed and 13 sibling** (shared ultimate parent, 9 groups over 20 LEIs), which are different facts and would draw different pictures. **Correction to P6.20 section 5:** its "genuine coverage limit of the publisher" is only partly that. Every Level-1 payload advertises, per parent kind, either a relationship-record or a REPORTING EXCEPTION, and across the 345 asked LEIs the split is **82 relationship / 251 exception / 12 neither** - the 82 reconciling exactly with the 154 edges. Both Telstra LEIs and Cultura Sparebank carry exceptions, so GLEIF published a REASON at an endpoint this project has never called. 9 asked LEIs also carry `successor-entity` links - potentially the one identity-PRESERVING GLEIF relation, deliberately left unmeasured and undecided. Classifies every relation as identity-preserving / identity-distinguishing / neither, proposes definitions including three deliberate REFUSALS ("parent company", "subsidiary", "controlled entity" are not defined and not used, because consolidation is not ownership or control), prices five policies, and measures the forbidden one: **relationship-implies-identity would take the shipped resolver from 3 to at least 33 false merges**, 13 of them fusing siblings such as `T-MOBILE US, INC.` with `T-MOBILE POLSKA SPOLKA AKCYJNA`. Recommends Policy D (non-merge constraint + a `consolidation` edge type named for what GLEIF publishes) staged behind collecting the reporting exceptions first. **Nothing enabled, nothing implemented, 7 items listed as requiring explicit owner approval.** | COMPLETE | AI | `Pending` | N/A | Memo only - no code change; all 44 numeric claims re-verified against the stored artifacts and raw payloads | N/A |
| P6.23.1 | **The UI milestone: a case overview, entity search, and dark-first actually delivered.** Parallel product track; the four P6.22 owner decisions remain unapproved and untouched. No backend, resolver, graph-semantics or data-model change - `src/lib/resolution/`, `src/lib/graph/build.ts` and every evaluation artifact are byte-identical to `af22018`, no endpoint was added, and no metric on any new surface is computed in a component: each is read from the server-derived state the shell already receives. **Fixed six real defects.** (1) There was no dashboard - "Overview" had been a permanently disabled rail entry since M10.2, so the product opened onto a stage-by-stage evidence log with no case-level answer to what exists, how much of it is fact, and what needs a human; the new Overview shows case identity, the investigator's path as five real stage states, and one card per stage, with a stated "not run" line rather than a zero for any stage that has not run, and an edges-by-classification census (1 Observed Fact / 156 Corroborated Fact / 34 AI Inference = the 191 real edges) so fact-vs-inference is answerable on the first screen. Deliberately NO "recent investigations" rail: this build ingests exactly one investigation and a list would be furniture. (2) There was no search anywhere - the only route to an entity was a `<select>` of every node inside the Graph toolbar, reachable only post-synthesis; entity search now sits in the command bar on every screen and as the Overview's primary entry, over the existing `GET /api/resolution/entities`, stating in as many words which set it covers, matching by plain case-insensitive substring over label/aliases/kind - no fuzzy matching, no ranking model, no embeddings - and fully keyboard operable. (3) "Dark-first" was never delivered: the palette hung off `@media (prefers-color-scheme: light)`, so every viewer on a light OS got the light theme; dark is now the default in fact, light is an explicit `[data-theme="light"]` applied before first paint. (4) **That exposed a real bug it also fixes** - sigma's `labelColor` defaults to black, so node labels were black-on-black on the dark canvas the product always intended; labels now read `--fg` and `resetTokenCache()` drops the canvas colour cache on a theme change. (5) The Graph screen spent ~380px on three stacked control bands before the canvas began and rendered nine identical solid accent pills: one toolbar row now, legend on demand, filters carrying the canvas's own `--kind-*`/`--edge-*` swatches with hidden facets dashed, dimmed, struck through and `aria-pressed` set. (6) Mobile was broken - a 240px rail on a 420px viewport with the case overflowing off-screen; the rail is an icon strip below `md`, the pipeline meter scrolls in one row, the flow steps wrap, the Inspector stacks below `lg`. Also: completed pipeline stages became navigation targets, and the Evidence screen stopped claiming six completed stages were "later milestones ... unavailable until then". Evidence remains the landing view and every `data-testid`, accessible name and label contract the e2e suite asserts on is intact. | COMPLETE | AI | `a7f4535` | `docs/progress/evidence/P6.23/` (6 before/after pairs, 5 new-surface captures, desktop + mobile + light) | 621/621 vitest, `tsc --noEmit` clean, `eslint .` clean, `next build` clean, 19/19 UI smoke checks driving the real app (including an assertion that the Overview's graph counts equal `GET /api/graph`) | N/A |
| P6.23.2 | **Pre-existing e2e failures identified and NOT fixed.** The Playwright suite reports 15 passed / 3 failed / 3 did not run. A pristine `git archive af22018` checkout run through the identical harness produces the **identical** 15/3/3 result, so the UI milestone changed the e2e outcome by exactly nothing. All three failures share one root cause - hard-coded corpus expectations that the resolver's own behaviour has since moved past: `investigation-resolution.spec.ts:99` expects `resolution-count-person` 10 and gets **17**; `investigation-synthesis.spec.ts:124` expects `graph-count-person` 10 and gets **17**; `investigation-topology.spec.ts:128` expects `analytics-count-ranked` 68 and gets **75**. The docs carry the same stale figures (`docs/data/graph.md` still states 68 nodes / 196 edges against an actual 75 / 191). Reported rather than corrected: fixing them means either changing resolution semantics or re-baselining evaluation expectations, and the UI milestone is explicitly forbidden from doing either. **This is a backend/evaluation dependency and needs an owner decision of its own, separate from the four P6.22 policy decisions.** | COMPLETE (report only) | AI | `Pending` | `docs/progress/evidence/P6.23/README.md` section 4 | Verified by differential run: same harness, same seed path, pristine `af22018` vs `a7f4535` | N/A |
| P6.24.0 | **P6.23 pushed; the remote is authoritative again.** `docs/repository-governance.md` states that GitHub is the single source of truth and that no implementation is complete until it exists on the remote, and the mandatory cycle is `implement -> validate -> commit -> push -> verify`. P6.23 had stopped at *commit*: `a7f4535` and `fbadf30` existed only on the local disk while `origin/master` still pointed at `af22018`, so a clone of the canonical repository showed no Overview, no entity search, no dark-first and no P6.23 evidence. Both commits pushed as a fast-forward (never a force-push, which branch protection blocks anyway) and `git ls-remote origin master` verified equal to local HEAD. **The two untracked paths were inspected and deliberately NOT committed**: `netintel-p6-handoff.tar.gz` is a handoff bundle of content already tracked, and `previous session/` holds superseded copies of P0/P6.1-era docs whose current versions are in `docs/`. Neither is required by any script, test or document. | COMPLETE | AI | `fbadf30` (push only) | N/A | `git ls-remote origin master` == `git rev-parse HEAD` | N/A |
| P6.24.1 | **The pairwise ER dataset, and the baseline correction that had to come first.** `scripts/ml/build-pair-dataset.ts` -> `evidence/ml/pair-dataset.json` (`cipher-er-pairs` v1.0.0, REAL). Collects nothing: inputs are the two P6.19 artifacts already in the repo, over GLEIF (SRC-002, CC0 1.0), Wikidata (SRC-001, CC0 1.0) and SEC EDGAR (SRC-006, US public domain), all APPROVED, all `direct-https`, raw payloads hashed and immutable. **The handoff's ~70.7% baseline was audited and is NOT the comparison figure**: it belongs to the P6.16/P6.17 India-filtered 75-pair corpus. On this corpus the shipped resolver is **40.7% (235/578)** corpus-wide and **44.4% (158/356)** on the held-out partition; using 70.7% would have compared across incompatible datasets. **4,053 pairs**: 578 positives and 146 curated hard negatives taken VERBATIM from the ground truth, plus 1,017 `mined_hard_negative` and 2,312 `sampled_negative` derived under the ground truth's OWN identifier rules - no label anywhere from name similarity, nothing manufactured. The mining exists because `build-expanded-corpus.ts` capped leading-token enumeration at `v.length <= 6`, so the corpus contained real hard negatives its own rule admits and its own enumeration skipped. The **anchored** corpus is used deliberately: it masks 748 of 1,245 records behind surrogate ids and withholds their identifiers, so the thing the label is made of is physically absent from most records the model sees. **All 154 GLEIF Level-2 consolidation edges are excluded from labels AND features** - P6.21.2's four decisions are unapproved, consolidation is not identity, and the semantics are frozen rather than guessed. 11 undetermined records, 79 former-name pairs (temporal, single authority - kept as a reported slice) and 94 not-comparable collisions excluded. | COMPLETE | AI | `Pending` | N/A | `tests/unit/ml-dataset.test.ts` (12) | N/A |
| P6.24.2 | **The leakage gate, which failed once and forced a rebuild.** `scripts/ml/leakage-audit.ts` -> `reports/ml/leakage-audit.json`, 10 checks, exits non-zero on any failure. Splits are **entity-disjoint at the SUBJECT level, never the pair**: subjects are grouped into 603 connected components before assignment so no labelled pair can straddle a boundary, and any component touching a subject the P6.19 ground truth already reserved as `heldout_evaluation` goes to TEST in whole - a subject reserved for evaluation is never trained on, which is why the held-out partition is 64% of the pairs and that is a consequence, not a choice. **L3 FAILED on the first run** and the split was invalidated and rebuilt, not argued with: `wikidata:EXP-0926` (Rocky Mountain Chocolate) is a positive partner of THREE subjects - one LEI and two CIKs, a predecessor filer and its successor - so assigning subjects independently put one record in validation and the held-out set at once. The builder now joins any subjects reachable through a shared record (4 joins) and any LEI/CIK pair a single record states together (92 bridges). **The central leakage finding: every label in this project is a function of identifiers, so an identifier feature is not a feature but the answer.** Enforced three ways - `FeatureRecord` has no identifier field (compiler), the anchored corpus withholds them (data), and L5/L6 assert the projection and grep the feature source (check). OpenCorporates id excluded on the same reasoning, `ocid_agrees` being co-determined with LEI agreement. Final: PASS 10/10. | COMPLETE | AI | `Pending` | N/A | `tests/unit/ml-dataset.test.ts` re-derives L1/L3/L4 independently | N/A |
| P6.24.3 | **Four experiments, the smallest adequate model shipped.** `scripts/ml/train-model.ts` reads ONLY train and validation - L10 greps the file for a held-out reference - and records every row whatever the outcome in `reports/ml/experiment-registry.json`. **E1** deterministic normalised-name equality, the shipped resolver's own Tier B/B2 rule replayed at pair level: 43.3% recall on validation. **E2** logistic regression on 25 engineered features: 86.7%. **E3** gradient-boosted trees (120 rounds, depth 3, lr 0.1, minLeaf 12, L2 1, posWeight 4): **90.0%**, shipped. **E4** an ABLATION adding `sameRegistry`: 91.7% - and its whole 5-point margin over E2 is an artefact of the labels having been built from cross-source agreement, which is why it is measured and NOT shipped. Trees beat the linear model because the failures being recovered are interactions a linear model cannot express: high token overlap is evidence of identity ONLY when the pair does not also look like a shared-leading-token family; the selection rule required >1 recall point to justify the harder explanation. Threshold chosen on VALIDATION as F1-max subject to a false-merge rate no higher than the deterministic resolver's on the same partition (0.00%). No ML dependency added - both learners are ~250 lines in `src/lib/ml/train.ts`, and the artifact is JSON that loads with `JSON.parse`. **A real serialisation defect was found and fixed**: `JSON.stringify(value, sortedKeys, 2)` applies its replacer ARRAY at every depth, so it silently deleted `parameters.weights` and every nested key, writing an artifact that parsed cleanly and could not score; replaced with a recursive canonicaliser, so the sha256 is a property of the content. | COMPLETE | AI | `Pending` | N/A | `tests/unit/ml-model.test.ts` (14), incl. serialise->parse->identical-scores and identical-hash | N/A |
| P6.24.4 | **The frozen held-out evaluation, run once, and the error analysis.** `scripts/ml/evaluate-model.ts` -> `reports/ml/heldout-evaluation.json` + `reports/ml/error-analysis.json`. 2,615 pairs, 356 positives, 2,259 negatives, 376 subjects, entity-disjoint from training. **Positive-pair recovery 158/356 (44.4%) -> 317/356 (89.0%), +44.7 points**, precision 98.8% -> 99.1%, F1 61.2% -> 93.8%, false-split 55.6% -> 11.0%, ROC-AUC 0.9822, PR-AUC 0.9482. **The cost is stated rather than averaged away: false merges 2 -> 3 (0.089% -> 0.133%), one additional pair.** Every false merge either model makes is inside the 114 curated hard negatives; on 2,145 mined and sampled negatives both are perfect. By class the baseline scores 0% on four families by construction and the model closes containment **0% -> 100% (98/98)**, script variant 0% -> 86.7%, partial overlap 0% -> 72.5%, divergent 0% -> 42.2%, with exact and legal-suffix unchanged at 100%. **THE FINDING: all three false merges are one problem.** `GENERTEL S.P.A.`/`Genertel` (0.999), `Cultura`/`Cultura Sparebank` (0.987), `BNP PARIBAS`/`BNP PARIBAS CARDIF POJISTOVNA` (0.985) are a group and one of its members - the exact pair shape P6.20.3 measured and P6.21.2 wrote its memo about, which Policy B would refuse at a measured cost of 0/578 true positives. That policy is unapproved, so **the model's entire false-merge residual is a decision the project has not yet taken**, and that is the bottleneck rather than data volume or model capacity. The model also REPAIRS two of the resolver's three known false merges, EN-0002 and EN-0003, scoring them 0.674 and 0.645 below a 0.897 threshold. 26 of 39 false splits are `divergent` translations and short names with no shared token (`LATVIJAS BANKA`/`Bank of Latvia`), which needs a different KIND of evidence, not a bigger model. No ground truth was altered. | COMPLETE | AI | `Pending` | `reports/ml/error-analysis.json` (42 rows: pair, truth, prediction, score, sources, category, fields, top features, deterministic result, next action) | `tests/unit/ml-dataset.test.ts` re-scores the whole held-out partition from the committed artifact and asserts the committed counts exactly | N/A |
| P6.24.5 | **The model inside CIPHER, as an advisory signal and nothing more.** `src/lib/ml/service.ts` and `POST /api/ml/pair-score`. **`src/lib/resolution/` is byte-identical to `af22018`** - no tier, threshold, rule or confidence changed, and no resolver code path calls the model. The one dependency runs the other way: the feature code imports the resolver's own `normalizeName`, so the two cannot drift in how they read a name. Output classification is **`algorithmic_signal`** - the project's existing vocabulary for a computed indication that is not a fact, the same class a spatiotemporal contradiction gets - so a model output structurally cannot be labelled `observed_fact` or `corroborated_fact`. Four load-bearing properties: the score never travels without all 25 feature values and their contributions; the deterministic verdict is always beside it and is PASSED IN rather than recomputed so the two cannot disagree on screen; the threshold comes from the artifact and is never invented at the call site; and the disclaimer names the group-and-member failure mode at the point of use, not only in a document. `assertFeatureContract` refuses to score when the artifact's feature list or ORDER disagrees with the build, because a silently reordered vector is the failure most likely to produce plausible nonsense. `scoreWithModel` is the single scoring path for both the evaluation script and the API, so inference reproducing evaluation is a property of the system rather than a claim. **No UI change, so no e2e surface is touched, and the three pre-existing P6.23.2 Playwright failures are neither fixed nor re-baselined.** No new dependency. Docs: data card, label specification, leakage audit, evaluation + error analysis, model card, reproduction path, ML integration. | COMPLETE | AI | `Pending` | N/A | `tests/unit/ml-features.test.ts` (12) + `ml-model.test.ts` (14) + `ml-dataset.test.ts` (12) = 38 new; full suite 659/659 | N/A |
| P6.25.1 | **A bigger real corpus, and the freeze made a ratchet.** Collected, dry-run gated and bounded, from the same three approved publishers: Wikidata 1,546 records in one SPARQL request (CC0 1.0), GLEIF 1,564 across 4 runs of 13 batched requests (CC0 1.0), SEC EDGAR 216 via the CIK bridge (US public domain). The Wikidata query gains ONE field, P17 -> P297, the ISO 3166-1 alpha-2 country code - the same vocabulary GLEIF's `jurisdiction` already uses, so two publishers become comparable without a mapping table of our own, and it is recorded in `labellingRules` as a FEATURE field that never creates a label. `scripts/build-corpus-v2.ts` merges EVERY collection run rather than the latest (reading only the latest, as the P6.19 builder did, would have discarded 1,678 of 1,743 GLEIF records) and keeps the P6.19 labelling rules character for character. **Scorable records 1,245 -> 3,290, positives 578 -> 1,711, hard negatives 146 -> 477, jurisdictions ~40 -> 126, validation positives 60 -> 177, validation curated hard negatives 4 -> 25.** THE RATCHET: a subject reaches TEST by its own designation OR by contagion through its connected component, and component boundaries MOVE when records are added - the first v2 build dropped five P6.24 frozen-test subjects into TRAIN and VALIDATION, and nothing in L1-L10 could see it because the new split is internally disjoint and disjointness says nothing about what an EARLIER frozen test contained. The builder now reads the P6.24 pair dataset back and promotes the 74 subjects that partition actually held to `heldout_evaluation`, and **leakage check L11 asserts the result rather than trusting the builder**. Reproduction of the published P6.24 numbers was verified FIRST, from the committed repository, before anything changed: dataset, model weights and all four evaluation reports regenerate exactly, differing only in embedded timestamps. | Completed | Claude (Claude Code) | `57e317a` | N/A | Yes - leakage 11/11 PASS on both datasets | No |
| P6.25.2 | **A one-way veto the leakage suite could not see, and the check that now catches it.** Scored on pairs it was never fitted on, the P6.24 model recovers **2.7% of positives at its own frozen threshold with PR-AUC 0.93** - ranking intact, operating point gone. Blanking one field restores it from 3/400 to 344/400. The field is `jurisdiction`: in the P6.24 corpus `jurisdictionBothKnown` was true for **0 of 222 positives and 196 of 1,216 negatives**, because Wikidata published none and every positive was cross-source WITH Wikidata, so the flag meant "same-source" and therefore "not a positive". The model learned it correctly for that corpus and catastrophically for any other. **L7 could not have caught this and cannot be tuned to**: it rates a feature by standalone ROC-AUC and passes anything in [0.01, 0.99], and a one-way indicator firing on 16% of one class and 0% of the other scores ~0.42, because AUC averages over a distribution and cannot see that one VALUE is a categorical veto. **L12 asks the other question directly** - is there a binary feature value which, with >=30 TRAIN rows of support, never co-occurs with one of the labels? Applied retrospectively the P6.24 dataset FAILS on four features, and `reports/ml/leakage-audit.json` is committed as it stands rather than regenerated into a pass. `officialNameBothPresent` was the same artefact in different clothes (only Wikidata publishes an official name, 531 of 3,282 records) and is now excluded from training, though the official name itself still feeds the variant comparisons where it is real evidence. | Completed | Claude (Claude Code) | `6599c70` | N/A | Yes - L12, plus a v1-artifact compatibility test | No |
| P6.25.3 | **The experiment ladder re-run at 3x the data, and a linear model wins on evidence.** `reports/ml/experiment-registry-v2.json`, seven rows, all recorded whatever the outcome. In P6.24 gradient-boosted trees beat logistic regression 90.0% to 86.7% recall on 1,044 training pairs and shipped; on 3,121 pairs the ordering **reverses and stays reversed through a capacity sweep** - E2 logistic 79.7%, E3 GBDT at the P6.24 settings 67.8%, E6 (300 rounds, depth 4) 33.9%, E7 (500 rounds, depth 5) 35.0%. The tree rows score HIGHER ROC-AUC and LOWER usable recall, the signature of a model whose ranking is fine and whose scores are too clumped near 1.0 for a false-merge-capped threshold to sit in; adding capacity made it worse, not better. Re-fitting the capacity to the new data size is the fair comparison - leaving the P6.24 settings stale and concluding "trees lost" would not have been. Two ablations, never shipped: **E4** registry-pairing sizes the artefact of labels built from cross-source agreement; **E5** masks the three jurisdiction features and sizes their total contribution (recall 81.9% but PR-AUC 0.9407 against E2's 0.9712). Two new features attack the measured failure without asserting any policy: **`legalFormConflict`** (read from the RAW name before normalisation, because normalisation strips exactly this token - `SIMON PROPERTY GROUP, INC.` and `SIMON PROPERTY GROUP, L.P.` both normalise to `simon property group` and are an UPREIT and its operating partnership with different LEIs; spellings of one form are grouped so `Limited`/`Ltd` is not read as a disagreement) and **`structuralTokenAsymmetry`** (one side carries a token naming a role inside a group that the other lacks; reads no relationship record). Threshold selection gains a **second ceiling over curated hard negatives alone**, because a ceiling over all negatives is nearly vacuous when 25 of 774 are hard - measured, not feared: the two features raised held-out recall 2.5 points and hard-negative false merges from 9 to 12 with the overall ceiling satisfied throughout. | Completed | Claude (Claude Code) | `6599c70` | N/A | Yes - 663 unit tests pass | No |
| P6.25.4 | **A final frozen test that has seen nothing, and the number it returned.** The v2 held-out partition stopped being a clean exam the moment its false merges informed feature design - selection never touched it, that was always validation, but DESIGN did, and reporting it as though it had stayed frozen would be the self-deception the leakage suite exists to prevent. So `scripts/build-final-test-corpus.ts` builds a third corpus from nine bounded country queries (IN, GB, FR, JP, AU, BR, ZA, SG and what they bridge to) collected AFTER all feature work, excluding every subject seen in any partition of either earlier dataset **at the RECORD level** - filtering only labelled pairs was tried first and left 1,563 of 2,520 subjects in place, because mined and sampled negatives are DERIVED from whatever records the corpus holds, so the positives were clean and the negatives were not. **5,257 pairs, 892 positives, 244 curated hard negatives, 963 subjects, 46 jurisdictions, overlap with anything previously seen: 0**, leakage PASS 12/12. Scored ONCE: **positive recovery 434/892 (48.7%) -> 682/892 (76.5%), +27.8 points**; precision 96.4% -> 93.7%; F1 64.7% -> 84.2%; **curated hard-negative false merges 16/244 (6.6%) -> 41/244 (16.8%)**. Both columns are the result. `scripts/ml/compare-models.ts` scores every model on pairs none of them was fitted on (0 of 5,257 excluded): deterministic 48.7%, P6.24 model **2.7%**, P6.25 model 76.5%, with near-identical PR-AUCs of 0.9335 and 0.9327 - which is why PR-AUC alone is never the acceptance criterion here. | Completed | Claude (Claude Code) | `6599c70` | N/A | Yes - leakage 12/12, 0-overlap assertion | No |
| P6.25.5 | **Error analysis: 46 false merges, ONE phenomenon, and a second finding not yet fixed.** `reports/ml/final-test-error-analysis.json`. **Every one of the 46 false merges on the final frozen test is a corporate-family pair** - not most, all, verified by inspecting the full list: `BARCLAYS PLC`/`BARCLAYS BANK PLC`, `ROLLS-ROYCE HOLDINGS PLC`/`ROLLS-ROYCE PLC`, `RELX GROUP PLC`/`RELX PLC`, `AMUNDI`/`AMUNDI ASSET MANAGEMENT`, `Virgin Australia`/`Virgin Australia Holdings`, `RENAULT`/`RENAULT SAS`, `CHRISTIAN DIOR COUTURE`/`CHRISTIAN DIOR`. These are distinct legal entities with distinct publisher-issued identifiers scored >=0.98, so no usable threshold separates them from true positives and calibration does not fix it. **That is the P6.21.2 question with a number attached, and it is the single reason the model stays advisory.** 209 of 210 missed positives are cases the deterministic resolver also misses. SECOND FINDING, recorded and deliberately NOT fixed here: the model recovers **6 of 106 cross-border positives (5.7%) where the resolver recovers 54 (50.9%)**, and is WORSE than baseline on edgar x wikidata (32.8% vs 65.6%). The cause is a semantics mismatch the jurisdiction features paper over - GLEIF publishes the legal jurisdiction of INCORPORATION (Jersey, Cyprus, BVI), EDGAR the US state of incorporation, Wikidata P17 the country the entity is ASSOCIATED WITH, so a "conflict" often means *incorporated offshore, operating onshore* (`CAPITAL COM SV INVESTMENTS LIMITED` (CY) and `Capital.com` (AU) are one company) while `jurisdictionCountryConflict` carries weight -1.12. **It was found by reading the final test, so fixing it by tuning against that test is not available** - that would spend the only unbiased instrument the project has, which is the mistake this phase existed to correct. The fix is to distinguish the two properties at collection time and re-measure on a fresh frozen test. | Completed | Claude (Claude Code) | `6599c70` | N/A | Yes - reproduced by `npm run ml:evaluate` on the final-test dataset | No |
| P6.25.6 | **The model inside CIPHER, still advisory, now with the measurement in the disclaimer.** `src/lib/ml/service.ts` ships `models/cipher-er-pair-classifier.v2.json` (E2-logistic-regression, 26 features, threshold 0.9774753387972909, weightsDigest `6948e6bc...`). **`src/lib/resolution/` remains byte-identical to `af22018`** - no tier, threshold, rule or confidence changed, and no resolver code path calls the model. The disclaimer now states the measurement rather than gesturing at a risk: 16.8% of genuine name collisions merged against the resolver's 6.6%, every error a corporate-family pair. THE FEATURE CONTRACT BECAME NAME-BASED: it was positional, which would have made dropping one leaky feature invalidate every artifact ever trained - and with it the ability to measure a new model against the one it replaces, which is precisely how the P6.24 model's 2.7% real-world recall was found. A name-based contract needs a different guard, since `featureNames` and `parameters` must agree as a unit, so every artifact carries a **`weightsDigest`** (sha256 over everything except `createdAt` and `gitCommit`) that `loadArtifact` verifies and refuses on mismatch; it doubles as the reproducibility test, because the file's own sha256 moves with those provenance fields and cannot answer "did this reproduce?". Documentation rewritten against committed artifacts: dataset card, label specification, leakage audit, model card, evaluation and error analysis, reproduction instructions, ML integration, and `docs/evaluation/README.md` - **which had claimed since P6.16 that evaluation was "Empty. No evaluation methodology has been defined."** | Completed | Claude (Claude Code) | `1e2e34d` | N/A | Yes - 663 unit tests, typecheck, lint | No |
| P6.25.7 | **Everything that still described the previous state, updated.** `docs/evaluation/README.md` had claimed since P6.16 that evaluation was "Empty. No evaluation methodology has been defined." while the directory held a leakage suite, a frozen evaluation and a shipped model - a stale index is worse than none, because it tells a reader who trusts it that measurements they could have used do not exist. Root README's ML section rewritten to the final-frozen-test numbers. **Three documents that ARGUED AGAINST training carry a superseded banner rather than a rewrite** - P6.18, P6.19, and `docs/training/training-feasibility-assessment.md` whose bottom line reads "nothing in this repository can be trained, and nothing in it should be". Their verdicts are superseded; their REASONING is not, which is why they are kept: the gate P6.18 defined is the one the model was eventually held to, and the residual P6.19 identified is still the model's principal limitation. Two of the feasibility assessment's conditions were **met rather than waived** - still no ML framework is installed (the trainer is plain TypeScript, the artifact is JSON) and Operation DarkNet Delhi has never been trained on. The **`ml:*` npm scripts now default to the SHIPPED pipeline**, with `ml:v1:*` for reproducing P6.24 and `ml:final-test:*` / `ml:compare` added; this is not tidiness - `npm run ml:leakage` previously ran the P6.24 dataset, which FAILS L12 by design, so a CI gate wired to it would have been permanently red because of a recorded historical finding. Earlier ledger rows are left exactly as written, `Pending` hashes included: the ledger is append-only and they record what was true when the work was done. Verified after the change: every default script reproduces the committed results, weightsDigest `6948e6bc` unchanged, regenerated artifact and reports differ in provenance fields alone; typecheck, lint, 663 unit tests and `next build` pass; live `POST /api/ml/pair-score` serves modelVersion 2.0.0 with 26 features. | Completed | Claude (Claude Code) | `1e2e34d` | N/A | Yes - 663 unit tests, typecheck, lint, build | No |
