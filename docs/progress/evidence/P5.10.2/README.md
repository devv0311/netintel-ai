# P5.10.2 — Command-centre visual system + shell: visual evidence

Evidence for ledger row **P5.10.2** (M10 / Workstream I), per
[`docs/progress/visual-evidence-convention.md`](../../visual-evidence-convention.md).

| Field | Value |
| --- | --- |
| **Ledger ID** | P5.10.2 |
| **Implementation status at capture** | In Progress (M10 milestone open; this task's artefacts complete) |
| **Associated Git commit** | `feat(P5.10.2): establish command-center visual system and shell` — the commit this evidence was captured against. |
| **Capture date** | 2026-09-03 |
| **Captured by** | [`capture.mjs`](capture.mjs), run as `node docs/progress/evidence/P5.10.2/capture.mjs after` (and `… before` under `git stash` for the comparison). |
| **Application state** | The real app (`next dev`) against `./data/cipher-e2e.db` — the full Operation DarkNet Delhi corpus ingested → extracted → resolved → graph-synthesized → analyzed → corroborated → dossier generated by the e2e suite. No mocking, no fixtures, no stubbed network. |
| **Theme** | The committed dark operational theme (`colorScheme: "dark"`, 1440×900). |
| **AI provider key** | Not configured. The Copilot screenshot therefore shows the deterministic no-key path, exactly as the e2e suite runs it. |

Every artefact is a real capture of the running system. Nothing here is a
mock, a placeholder, or a design comp.

## Artefacts

| File | What it shows |
| --- | --- |
| `P5.10.2_screenshot-command-bar_…png` | The command bar: product identity (`CIPHER` / `Investigation Workspace`), the loaded case's name + corpus version, the live 9-stage pipeline meter (real `completedStages`), and the non-removable synthetic-data marker. |
| `P5.10.2_screenshot-nav-rail-expanded_…png` | The navigation rail, expanded: the journey grouped `Case / Analysis / Assist / Report`, with `Overview`, `Timeline` and `Map` disabled (no backing stage) and the rest enabled from real pipeline state. |
| `P5.10.2_screenshot-nav-rail-collapsed_…png` | The same rail collapsed to the icon strip (preference persisted per browser; accessible names preserved). |
| `P5.10.2_screenshot-shell_…png` | The whole shell in one frame — command bar + rail + workspace — in the dark operational theme. |
| `P5.10.2_screenshot-classification-treatments_…png` | `ClassificationChip`, the five evidence classifications side by side: each now has its own fill / text / border-style / glyph, replacing the old treatment that gave three of the five an identical outline. Rendered from the real component (component preview). |
| `P5.10.2_screenshot-provenance-block_…png` | `ProvenanceBlock` — the six required provenance fields as a mono `[key │ value]` grid with the audit-trail left border (component preview). |
| `P5.10.2_screenshot-states_…png` | `EmptyState` / `LoadingState` / `ErrorState`, the shared empty/loading/error primitives (component preview; broader adoption across screens is M10.3+). |
| `P5.10.2_screenshot-dossier-census_…png` | Real Dossier screen: the classification census now rendered with the five distinct `ClassificationChip`s + counts — classification labels are not flattened for cleanliness. |
| `P5.10.2_screenshot-dossier-classification-provenance_…png` | Real Dossier finding, expanded: the `ProvenanceBlock` six-field chain ending at `dossier:assemble`, plus cross-navigation. |
| `P5.10.2_screenshot-copilot-classification-provenance_…png` | Real Copilot answer (no-key deterministic path): per-claim `ClassificationChip` + confidence, and the provenance / derivation panel (kept intact — it is a superset of the six fields). |
| `P5.10.2_comparison_…png` | Side-by-side, pre-M10.2 shell vs M10.2 command-centre shell (command bar over full shell), captured from the same route and data with the change stashed / restored. |
| `P5.10.2_recording-shell_…mp4` | Screen recording of the shell: landing, the pipeline meter, and collapsing / expanding the navigation rail. |

## Notes

- **Recording format.** Playwright records `webm`; it was converted to
  `mp4` with `ffmpeg` for this row. The source `webm` is not kept.
- **Component previews vs integration screenshots.** The chip / provenance
  / state artefacts are the real components rendered on a throwaway route
  that `capture.mjs` creates and deletes — the state primitives are not
  yet mounted in a screen (that adoption is M10.3+). The Dossier and
  Copilot artefacts are the real screens with the primitives integrated.
- **Regression evidence.** `npx tsc --noEmit`, `npx eslint .`,
  `npx vitest run` (462/462), `npx next build`, `npx playwright test`
  (20/20) and `git diff --check` all pass on this commit; the e2e suite is
  unchanged.
